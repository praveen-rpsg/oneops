# OneOps — Platform Build Plan (Canonical Execution Tracker)

> **This is the single source of truth for what OneOps is becoming, what is
> done, what is in progress, and what to build next.** Every session and every
> agent reads this FIRST and updates it as work lands. If you are a new
> session/agent: start here, find the `▶ CURRENT` marker, do that, then move the
> marker. Do not re-plan from scratch — refine this in place.

**Owner:** CTO (top-level session). **Status convention:** `[x]` done & merged ·
`[~]` in progress (branch open) · `[ ]` not started · `▶` the current front.
**Last updated:** 2026-08-06.

---

## 0. How to use this file (read this every time)

1. **Before building anything**, read §3 (cross-cutting standards) and the epic
   you're touching. Every increment honors §3 — no exceptions.
2. **Find `▶ CURRENT`.** That is the next thing to build. One front at a time per
   dependency chain; independent epics may run in parallel on separate branches.
3. **Build → review → CTO commit → merge.** Nothing merges on an implementer's
   own claim. Commit to the branch BEFORE review (a reviewer's mutation-cleanup
   `git restore` must never wipe uncommitted work — this bit us once).
   **Calibrated review depth (2026-08-03):** a FULL independent mutation-testing
   review agent for any increment touching security, tenant-isolation,
   concurrency, data-integrity, or hardened migrations (where the gate has caught
   every real breach). A FAST CTO self-review (verify build + tests + RLS +
   contract + guards directly, no separate agent) for genuinely low-risk additive
   increments (CRUD, DTOs, read-only projections, docs). Safe parallelism via
   worktrees is unavailable here (repo is the `oneops/` subdir, not the workspace
   root), so increments run sequentially — calibrated depth is the speed lever.
4. **When an item lands, update its checkbox here and move `▶`** in the same
   change. A stale plan is the failure this file exists to prevent.
5. **Scope discipline:** an increment is one coherent, reviewable story. If a
   brief would exceed that, split it and list the splits here.

---

## 1. Vision & scope

OneOps is **one unified, AI-native enterprise operations platform** — a single
operating model spanning governance, IT service management (ITSM), network
operations (NOC), security operations (SOC), asset/configuration management,
observability, automation, and executive decision support. It is the real
product; the earlier `AINOC` effort is retired and out of scope.

Bar: **best-in-the-world, not a lollipop product.** Every module is
multi-tenant, secure, scalable, observable, evidence-backed, and honors the
constitution. We build depth (edge cases, failure modes, operability), not demos.

## 2. Foundations already in place (do not rebuild — extend)

The control plane on `master` already provides the substrate every module reuses:

- **Identity:** JWT + multi-IdP SSO (OIDC/JWKS) with issuer→tenant binding; RBAC;
  Organizations, Tenants, Teams, Memberships, Users, Invitations.
- **Tenancy:** row-level security on every tenant-owned table (`TenantOwnedTables`
  + FORCE RLS), re-derived ownership, startup + sentinel schema validation.
- **Data & governance:** Configuration objects + a **dependency-graph engine**
  (`internal/graph`: WalkDependencies/Dependents/DetectCycles) — the CMDB reuses
  this. Governance state machine (lifecycle + transitions). Append-only,
  tamper-evident **audit** chains. Optimistic-locking/versioning everywhere.
- **Work & automation:** Policy automation (event→Action), **Workflow as
  action-decision composition** (multi-step, gated, self-healing, crash-durable),
  **multi-approver approval quorum**, **Notification service** (at-least-once,
  leader-gated), Webhooks (SSRF-guarded, at-least-once).
- **Platform:** Settings (per-tenant), per-tenant rate limiting, observability
  (metrics/tracing/health), PrometheusRule alerting/SLOs, Helm (HPA/PDB/
  NetworkPolicy/TLS/ServiceMonitor), DR/backup, Go SDK, embedded web console,
  **Platform Knowledge Graph (PKG)** (derived architectural truth).

These are the Lego bricks. **Compose them; don't re-found them.**

---

## 3. Cross-cutting standards — EVERY increment honors these

These are the "all angles" that make it world-class rather than a demo. A review
checks them on every module.

**Correctness & governance**
- Multi-tenant isolation: every tenant-owned table in `TenantOwnedTables` + FORCE
  RLS + fail-closed policy; a two-tenant negative test that BITES.
- Audit: every meaningful mutation is attributable and, where constitutional,
  append-only. Optimistic locking (RowVersion) on mutations.
- Ratified vocabulary + reduced-concept discipline (see §4). No guard weakened;
  new decisions get an ADR.

**Scale & performance**
- Designed for the data volumes each domain implies (telemetry is the extreme —
  see E2). Pagination, filtering, and bounded queries on every list endpoint.
  Index every access path; no unbounded scans on hot paths. Load/latency budget
  stated per module; regressions caught by the load harness (`make loadtest`).
- Backpressure, retries with capped backoff, idempotency (deterministic ids +
  ON CONFLICT), leader-gated singletons, at-least-once where delivery matters.

**Reliability & operability**
- Crash-durable (self-healing sweeps, not permanent stalls). Graceful shutdown.
- Every module emits its own metrics + health; every alertable failure has a
  runbook. DR/backup covers new stores.

**API & UX consistency**
- Uniform REST under `/v1`, exact OpenAPI contract (bijection test), consistent
  errors (RFC 7807), pagination, filtering, sorting. SDK + console coverage per
  module. Real-time surfaces (NOC, dashboards) use a defined streaming layer (E11).

**Security**
- AuthZ on every resource (RBAC + tenant scope). Secrets never inline. Outbound
  calls SSRF-guarded. Security-relevant classes follow the Trust-Register
  discipline (live exploit → fix → re-attack → guard → ADR).

**Extensibility**
- Ingestion/integration is connector-based (E12), not hard-coded per vendor.
  Import/export and bulk ops on core entities.

**Edge cases are first-class.** Each epic below lists the non-obvious ones. "It
works on the happy path" is not done.

## 4. Reduced-concept handling (constitutional — do not reify)

Vol II §5.3 / Vol III §3.4 ratify these as **derived/projections, never stored
domain types**: `Alert`, `Event`, `Signal`, `Dashboard`, `Report`, `Ticket`,
`Metric`, `Runbook`, `State`, `Workflow`. Deliver the CAPABILITY over primitives:
- **Alert/Event/Signal** → derived from telemetry crossing rules; correlated into
  incidents. Not a stored `Alert` table-as-noun; a rule + a derivation + a
  notification/incident.
- **Ticket/Incident/Problem/Change** → stateful work items on the governance
  state-machine + workflow (lifecycle, assignee, transitions, approvals).
- **Dashboard/Report** → queries/projections over audit, timeline, telemetry,
  and the domain stores.
- **Workflow** → already delivered as action-decision composition.
If a capability truly cannot be delivered without reifying a reduced concept,
that is a founder-level constitutional override + ADR, not a story decision.

---

## 5. THE BUILD PLAN (sequenced, foundation → operations → intelligence)

Dependency order matters: you cannot monitor, alert on, ticket against, or secure
what you cannot model. Assets first; intelligence last.

### E1 — Asset / CMDB foundation  `[x]`
The typed configuration-item model + relationship graph everything references.
- [x] E1.1 CI model + relationships + CRUD/graph API (reuses `internal/graph`) — **merged to master** (ADR-ASSET-001; cross-tenant edge defense mutation-proven)
- [x] E1.2 Business-service mapping (service → supporting CIs), criticality, environment, ownership — **merged** (owner refs tenant-verified; service-map = typed graph projection)
- [x] E1.3 CI lifecycle (planned→active→retired), change history, soft-retire — **merged** (4-state machine per amended ADR-ASSET-001 §5; append-only history hardened ENABLE ALWAYS+REVOKE)
- [x] E1.4 Bulk import/export + duplicate **detection** (source+external_ref, idempotent upsert, DB uniqueness) — **merged**
- [ ] E1.4b Controlled duplicate **merge/resolution** (reassign relationships+history to a survivor, retire duplicate) — deferred follow-on, does not block epic completion
- [x] E1.5 CMDB health: staleness, orphans, data-quality (`last_seen` freshness field + GET /admin/assets/health) — bounded, index-backed, RLS-isolated; true drift-vs-reality deferred to post-E2 discovery. **E1 (CMDB) is now complete** — E1.4b remains a separately-gated later increment.
- [ ] FOLLOW-UP (tech-debt): harden `TestEveryAuditAppendPath_SerialisesOnItsChainHead` — `reachesForUpdate` keys on unqualified method name, so a `Create` can pass via cross-type name-collision (pre-existing; not introduced by E1.x). Also: relationship export (E1.4 exported assets only).
- **Edge cases:** circular relationships, cross-tenant edges (forbidden), orphaned/duplicate CIs, high-fan-out services, historical point-in-time queries.

### E2 — Telemetry & Monitoring ingestion  `[ ]`
Signals about assets: metrics, logs, traces, synthetics. The scale extreme.
- [x] E2.1 Ingestion data model + API (metrics/time-series tied to CIs), spine only — **merged** (ADR-TELEMETRY-001: Postgres + TimescaleDB hypertables behind `domain.TelemetryRepository`, D1 resolved; `telemetry_sample` tenant-owned + RLS + FORCE; `asset_id` tenant-re-verified on write per-sample, same defense as ADR-ASSET-001 §6; bounded ingest batch (1000) + bounded/paginated range query (5000, keyset over timestamp); infra-gate proved the Timescale image swap regresses nothing in the pre-existing suite)
- [x] E2.1b Retention + downsampling over telemetry_sample — **merged (`1757dc6`)**. FINDING: TimescaleDB's `add_retention_policy`/continuous-aggregate features require the Timescale License; the pinned image (`*-oss`, ADR-TELEMETRY-001) carries only Apache — verified live. ADR-TELEMETRY-002 records the (in-scope) alternative actually shipped: `TelemetryRetentionWorker`/`TelemetryRollupWorker` drive Apache-licensed `drop_chunks`/`time_bucket` directly; downsampling is a second platform-owned hypertable (`telemetry_rollup_5m`) carrying the SAME RLS as every tenant-owned table (stronger tenant-isolation position than a native continuous aggregate, whose backing object is unproven under RLS — mutation-tested live). Raw retention horizon is an operator-tunable Setting (`telemetry_raw_retention_days`, platform-scoped — per-tenant retention deferred, documented, not faked). `QueryRange` gained a `resolution` param (raw/rollup_5m/auto) with correct wide→rollup, narrow→raw selection. Also folds the E2.1 review nits (first-write-wins documented; pagination/natural-key comment). Relicensing to the Timescale-licensed image for native policies is a separate, undecided ADR-level choice.  **DONE + merged (`1757dc6`, OPS-TELE-002).**
- [x] E2.1b Retention + downsampling — **merged** (ADR-TELEMETRY-002: Apache-primitive workers; rollup is a 2nd owned RLS hypertable; OneOps stays Apache-only for telemetry — standing decision). Follow-ups: PKG extraction-budget guard tightening (217/200ms under load); retention-error metric/alert; setting-decode diagnostic log.
- E2.2 Agentless collectors — SPLIT:
  - [x] E2.2a Collector framework + HTTP uptime check — **merged** (leader-gated scheduler, SSRF-guarded via safehttp at DIAL time [DNS-rebinding refused], tenant-verified asset, RLS; writes up/latency/status telemetry). SECURITY-GUARD FOLLOW-UP (tracked): the SSRF arch guard `TestOutboundClients_AreSSRFGuarded` gives illusory coverage — it only catches a bare `&http.Client{}` LITERAL and doesn't sweep `cmd/` (the composition root), so it can't catch an SSRF regression for ANY of the 3 outbound clients (webhook/registry/collector). Shipped code is safe; harden the sweep to include cmd/. Also: up=any-status masks 5xx (future health-semantics); due-scan starvation at very high check counts.
  - [ ] E2.2b SNMP collector (network devices) — protocol dep + credential (community/v3) storage
  - [ ] E2.2c Cloud/API pollers (AWS/etc.) — credential storage + connector pattern (see E11 connector framework)
- [ ] E2.3 Agent/push ingestion (app/APM, custom metrics), log ingestion
- [ ] E2.4 Distributed tracing ingestion; correlation to CIs
- **Edge cases:** high cardinality, ingestion backpressure/quotas per tenant, out-of-order & late data, clock skew, collector disconnection, storage growth — none of these are addressed by E2.1's spine and remain open for E2.1b/E2.2/E2.3.

### E3 — Alerting (derived) + suppression  `[x]`
- [x] E3.1 Alert **rules** (threshold first; rate-of-change/absence/anomaly later) over telemetry + leader-gated evaluator that fires on transition → Notification. `AlertRule` is config; a firing is DERIVED (no reified `Alert` type — §4). Cross-tenant telemetry isolation enforced by an explicit tenant_id predicate (`TelemetryRepository.QueryRangeForTenant`), mutation-verified.
- [x] E3.2 Flap suppression: a candidate transition must hold continuously for the rule's own `flap_dwell_seconds` (default 60s, rule-level config, patchable) before it is committed/notified — hysteresis DWELL, not a blind cooldown (ADR-ALERTING-001). Oscillation collapses to at most one eventual transition; a genuinely sustained change still transitions promptly. Severity is unaffected — still `AlertRule.Severity`, attached at commit exactly as E3.1/E4.1 already do. `pending_state`/`pending_since` (evaluator-only bookkeeping, not a new entity, not in the HTTP contract) persist the in-flight candidate on the SAME `alert_rule` row so the dwell clock survives a restart/leader failover; any admin edit clears it. Mutation-verified (short-circuiting the dwell check fails 3/4 new tests).
- [x] E3.3a Maintenance windows / blackout: operator-declared, one-shot `[starts_at, ends_at)` window over a single asset during which an `ok→firing` transition's incident-create/link and notify are suppressed — the underlying firing derivation and E3.2's dwell/pending bookkeeping are untouched, and recovery is never suppressed (ADR-ALERTING-002). Tenant-owned `maintenance_window`; the evaluator's privileged active-window check carries an explicit `tenant_id` predicate (ADR-TENANCY-012); suppression is recorded (`suppressed_count`/`last_suppressed_at`), never silently lost. A still-firing condition pages on the next tick after its window ends. Split out of the old bundled E3.3 — lower-risk, universally-needed, shipped first. Mutation-verified (defeating the check flips 6 tests to FAIL). Reviewer APPROVE-WITH-NITS; nits folded (ADR count, `suppressed_count` per-(rule,tick) semantics, guard blind-spot note below).
- [x] E3.3b Dependency-aware suppression (ADR-ALERTING-003): when a CI X would fire and a CI **X depends on** (`depends_on`/`runs_on`, cycle-safe recursive-CTE traversal, max-depth 10) is itself "down" (≥1 enabled+firing `alert_rule` on it), X's `ok→firing` side-effect is suppressed — mirroring E3.3a exactly (skip RecordTransition+incident+notify; state untouched ⇒ **self-clearing** when the root recovers). Down-check is a plain privileged SELECT with an explicit `tenant_id` predicate (now actively covered by the E4.1-GUARD read guard); traversal stays RLS-scoped. Recorded in tenant-owned `dependency_suppression` (names the root, atomic upsert). Precedence: maintenance-window (E3.3a) checked first. **HONEST BOUND:** no rule symptom-class exists ⇒ all-or-nothing per asset (can mask a genuinely independent failure on X while a dependency is down); mitigated by visible recording + self-clearing; symptom-class scoping named as future refinement. Direction-correctness + both-tenant-isolation + cycle + edge-type-exclusion mutation-verified (store, real PG) and evaluator hook mutation-verified (defeating the gate flips 4 tests). **VERIFICATION (honest record):** 3 implementer-agent API drops during build + 2 reviewer-agent drops during review (session-wide API instability), recovered via WIP-to-branch each time. Evaluator unit tests + ADR authored by CTO after the 3rd build drop. The independent reviewer confirmed baseline+contract+vet+lint green before it dropped; the CTO then completed the adversarial gate directly: all 4 mutation proofs re-run with a REAL integration DB (direction flip → dir test fails; tenant predicate break → store isolation fails; guard tenant-strip → E4.1-GUARD fails naming this store; evaluator gate defeat → 4/9 flip), plus an adversarial read of the agent-written store logic. **This caught 2 real integration failures an earlier skip-run had hidden** (dependency_suppression_pkey unregistered in the tenant-key-scope + uniqueness guards — fixed: server-minted suppression_id justified, mirroring maintenance_window). Full real integration suite (race) now 0 FAIL / 30 pkgs.  [merged — see below]
- [x] E3.4 **Alert-rule symptom-class taxonomy** (ADR-ALERTING-005) — `alert_rule.symptom_class` enum = **`availability | resource | unspecified`** (minimal — the one distinction suppression/correlation need: does the symptom cascade through dependencies? availability does, resource doesn't). **Explicit operator-set field, default `unspecified`** (CTO call on founder's "follow best"): existing rules backfill to `unspecified` via the migration's `DEFAULT` and behave EXACTLY as today — no metric-name inference (too fragile / silently mis-suppressing). Optional on create, patchable via `AlertRulePatch.SymptomClass`. **E3.4 = the PRIMITIVE ONLY** — field + create/patch + backfill + validation (domain `Validate` + DB `CHECK ck_alert_rule_symptom_class`, both mutation-verified) + DTO/openapi (additive, `contract-breaking` clean) + PKG census bump. **NO behavior change**: `internal/alerting/evaluator.go`, `internal/grouping`, dependency_suppression and maintenance_window were not touched, and their full pre-existing suites pass unmodified — nothing reads this column to decide anything yet. The class-scoped refinements of E3.3b dependency suppression and E4.2 root-cause ranking are SEPARATE follow-on stories that consume this primitive. NOT a reified entity — a column on `alert_rule`. Round-trip (each class + omitted⇒unspecified) + backfill + DB-CHECK-rejection mutation-verified against real Postgres. Full unit suite 1145 PASS/0 FAIL; full integration suite (race) 1518 PASS/0 FAIL/3 pre-existing unrelated SKIPs.
- **Edge cases:** alert storms, self-referential suppression, rule changes mid-fire, per-tenant rule isolation.

### E4 — Event Correlation & AIOps-lite  `[x]`
- [x] E4.1 Correlate an alert firing into an incident: create-or-link by affected CI (connects E3→E5). `Incident.Source` (manual|alert) gates correlation so an operator's own incident is never auto-annexed; `alert_rule.current_incident_id` is the LINK (a column, not a reified Alert/Event). Wired into the evaluator's ok→firing (find-or-create, DB-uniqueness-backed — no duplicate even under true concurrency) / firing→ok (append recovery note, clear link, never auto-resolve/close) transitions. Every privileged correlation read/write carries an explicit `tenant_id` predicate (ADR-TENANCY-012), mutation-verified (`AppendAlertNote`'s tenant check bites; the no-duplicate unique index bites). READ-SIDE GUARD still NOT built (tracked below, ADR-TENANCY-012's own follow-up) — flagged rather than forced: a general sweep risks false positives against every dual-role store (AlertRuleStore/CollectorCheckStore/IncidentStore) whose SAME methods serve both the tenant-scoped and privileged pools depending only on which pool constructs them, which the type system cannot currently distinguish.
- [x] E4.1-GUARD (carried over) Build the failing arch guard sweeping privileged `SELECT`s on `TenantOwnedTables` for an explicit tenant predicate (ADR-TENANCY-012's read analogue of `TestPrivilegedMutations_AreScopedToAnOwner`). **DONE + merged (`2a8f193`)** as `arch.TestPrivilegedReads_AreScopedToATenant`: derives privileged store types from the composition root (wiring-level, like `TestServerWiringUsesTenantScopedPool`, not a per-method text sweep), flags any `asset_id`-scoped privileged read lacking a `tenant_id` predicate; mutation-proven to bite (incl. concat-column style); canary-guarded against vacuity; 6 justified exemptions. ADR-TENANCY-012 follow-up discharged.
  - **DEBT (arch guard — CTE-with-UPDATE tenant-read coverage; found E3.3a review 2026-08-05):** neither static guard enforces the `tenant_id` predicate on a read embedded in a write-CTE (a statement containing `UPDATE <table>`) — the read guard skips it as the mutation guard's subject, and the mutation guard only fires on the `id = ANY($)` shape such methods lack. Affects `MaintenanceWindowStore.Suppress` and `IncidentStore.FindOrCreateOpenAlertIncident`; both are correctly predicated and covered by live cross-tenant integration tests, so this is a static-coverage gap, not a live hole. Honest bound recorded in `privileged_read_test.go`'s doc comment. Closing it = teach the sweep to parse the SELECT portion of a write-CTE. Do before the next privileged write-CTE reader lands.
- [x] E4.2 Topology-aware incident grouping (ADR-ALERTING-004): a nullable self-FK `incident.root_incident_id` — NOT a reified Group/Correlation noun (§4) — set/cleared exclusively by a leader-gated `internal/grouping.Reconciler` periodic pass (mirrors `alerting.Evaluator`'s own Config/Store/Run/RunOnce shape), never an HTTP write path. Heuristic (ML later in E13): for an open alert-incident on asset X, walk X's transitive `depends_on`/`runs_on` closure (identical edge-type filter + direction discipline to E3.3b) for nodes that are themselves "down" (their own open alert-incident); the DEEPEST such node's incident wins (ties broken lexicographically by asset_id), producing FLAT grouping directly since the closure is already fully transitive. No down dependency ⇒ NULL. Self-healing + idempotent by recomputation, not locking: a resolved root's children re-root or clear on the very next pass; an unchanged pass writes nothing. A genuine CMDB dependency cycle (not a stored-pointer bug) is broken deterministically (lexicographically-smallest incidentID anchored to NULL), never persisted as a pointer cycle; a same-row self-pointer is additionally refused at the schema level. Grouping never mutates incident STATE — the only write, `SetRootIncidentID`, cannot express one. Privileged store (`IncidentGroupingStore`) carries an explicit `tenant_id` predicate on every read/write, mutation-verified on both; the dependency walk stays RLS-scoped (tenant-scoped pool, `domain.WithTenant` re-bound per tenant per pass) — proven independent even when two tenants share identical `asset_id` strings with different topologies. Additive read-only DTO projection (`root_incident_id` omitempty + `is_root`); no new group-management API. **HONEST BOUND:** single-root pick (not ranked/multi-root); heuristic, not ML; a silently-dead root with no open incident of its own is invisible to it (same limitation class as E3.3b's down-check); no symptom-class exists yet (E3.4, founder-approved but not built) so grouping cannot distinguish a genuinely independent coincident failure from true collateral — named future refinement, not built here; eventually-consistent (default 60s reconciliation interval, not instant). Complementary to, and does not replace, E3.3b: suppression prevents a NEW collateral page at transition time when a root is already firing; grouping organizes the incidents that get created anyway (a root detected later, a suppression window that did not apply).
- **Edge cases:** correlation never crosses tenants (E4.1 done); window tuning; late-arriving correlated signals; grouping tenant isolation + no-state-mutation + cycle-safety all mutation-verified (E4.2 done).

### E5 — ITSM core: Incident / Problem / Change  `[ ]`
Stateful work items on the workflow + state-machine primitives.
- [x] E5.1 Incident lifecycle (manual and, since E4.1, alert-correlated), assignment, append-only timeline *(sequenced ahead of E3.2/E4: after "notify", operators need to TRACK work — incidents are the NOC's operational heart; independent of alerting refinements)*
- [x] E5.2 On-call, scheduling & paging — **SPLIT (CTO, 2026-08-05) into E5.2a + E5.2b** (founder sequence: E3.4 → E5.2):
  - [x] E5.2a **On-call schedules & rotations (who's on call now).** Tenant-owned `on_call_schedule` (name, `handoff_interval_seconds`, `rotation_start_at`, status) + `on_call_participant` (ordered users; FK `app_user` re-verified via ACTIVE membership per ADR-IDENTITY-002, like incident-assignee). Who's-on-call(T) = **deterministic computed rotation** `index = floor((T − rotation_start)/interval) mod participants` — NO JSON rules engine, NO per-shift rows (that's a framework). CRUD + reorder + a "who's on call now" endpoint (`GET .../on-call-schedules/{id}/on-call?at=`). Reduced-concept safe (legitimate operational objects, not false-nouns). No privileged pool / background worker — request-time resolution only (ADR-ONCALL-001). `AddParticipant` re-verifies ACTIVE membership (mirrors `IncidentStore.verifyAssigneeExists`, mutation-verified to bite); reorder is atomic and set-checked (`uq_on_call_participant_schedule_position` DEFERRABLE INITIALLY DEFERRED to allow a genuine position swap); positions stay contiguous 0..N-1 across add/remove/reorder. Rotation math is a pure, DB-free function (`domain.OnCallRotationIndex`) covering N=0/1/3, the half-open handoff boundary, before-rotation-start, and far-future inputs; the positive-modulo formula is separately, directly mutation-tested (`positiveModulo`/`TestPositiveModulo`) since the public function's own guard keeps every live input non-negative. **Deferred (honest bound):** overrides/coverage-swaps, multi-layer follow-the-sun, calendar-aware (non-UTC-seconds) cadences. Self-contained — no escalation dependency. Full unit suite (race) 1183 PASS/0 FAIL; full integration suite (race) 1562 PASS/0 FAIL/3 pre-existing unrelated SKIPs.
  - [x] E5.2b **Escalation policies & paging** — **SPLIT (CTO, 2026-08-05) into config + engine** (config is low-risk additive CRUD; the engine is a concurrency-heavy state machine deserving full independent review):
    - [x] E5.2b-1 **Escalation policy + tier config (CRUD).** Tenant-owned `escalation_policy` (name, status; MVP = one tenant-default) + ordered `escalation_tier` (position, `on_call_schedule_id` FK = which schedule to page, `wait_seconds` before advancing). CRUD + tier reorder (reused E5.2a's deferred-position-unique pattern exactly: `uq_escalation_tier_policy_position` DEFERRABLE INITIALLY DEFERRED, `ReorderTiers` mirrors `ReorderParticipants` line for line). `escalation_tier.on_call_schedule_id` re-verified same-tenant on the writer's own RLS-scoped connection before insert — the FK alone bypasses row-level security via the FK trigger's own privileges (ADR-ASSET-001 §6), mutation-verified to bite. NO engine/worker/paging/incident/notification wiring — that is E5.2b-2 entirely. ADR-ONCALL-002. Full unit suite (race) green; full integration suite (race, real Postgres) green, including 9 escalation-specific integration tests (CRUD, two-tenant RLS isolation, cross-tenant-schedule rejection mutation-verified, append ordering, removal compaction, atomic reorder with a genuine swap, mismatched-reorder rejection, cascade delete, `wait_seconds>0` DB CHECK backstop). Schema census/key-scope/uniqueness/wiring/contract guards all green; `make contract-breaking` additive-only.
    - [x] E5.2b-2 **Escalation engine (seed → page → escalate → cancel).** Tenant-owned `escalation_state` work-queue (at most one row per incident: `UNIQUE (tenant_id, incident_id)`, `current_tier_index`, `next_attempt_at`, `claimed_at` fence, status active|acked|resolved|exhausted — claim/fence shape mirrors `notification` exactly, ADR-CONCURRENCY-002/005/006/007). A leader-gated **Seeder** enrols open, unacked, alert-sourced incidents at the tenant's default policy (single active policy, or earliest-by-`created_at` if several; no active policy ⇒ nothing seeded, not an error) — a SEPARATE reconciliation pass, alerting hot path untouched (mirrors E4.2's grouping-reconciler precedent). A leader-gated **Worker** mirroring `notification.Worker` (ClaimDue → re-load incident, still open? if acked/resolved-class mark terminal & stop (this is how ack/resolve cancels, re-checked every cycle) → resolve who's-on-call for the tier's schedule → **PAGE-TIME ACTIVE-MEMBERSHIP RE-CHECK (E5.2a review nit 1, new public `MembershipStore.ActiveMember`): do NOT page a revoked/absent user — skip and advance anyway, recorded via `IncSkippedRevoked`, mutation-verified to bite live against Postgres** → enqueue an **email** Notification to the on-call user's resolved address (no `internal/notification` change) → advance tier or mark exhausted). Every tenant-scoped read bound to the claimed row's own tenant via `domain.WithTenant`, reusing the EXISTING tenant-scoped incident/policy/on-call/membership/user stores unchanged rather than a privileged reimplementation (ADR-TENANCY-003). No double-page under concurrent claims (`FOR UPDATE SKIP LOCKED` + fenced `Advance`/`MarkTerminal`, mutation-verified with a forced two-worker race and a forced stale-claim fence). Two-tenant RLS isolation mutation-verified live (a connection bound to tenant B cannot read tenant A's real `escalation_state` row). **Deferred (honest bound):** per-asset/service policy routing (MVP tenant-default), SMS/push, re-arming escalation on a reopened incident that already ran to a terminal state. ADR-ONCALL-003. Full unit suite (race) 1718 PASS/0 FAIL/6 pre-existing unrelated SKIPs; full integration suite (race, real Postgres) 439 PASS/0 FAIL/2 pre-existing unrelated SKIPs (perf tests excluded under `-race` by design). Schema census (43 tables/402 columns/90 indexes/140 constraints, 37 tenant-scoped)/key-scope/uniqueness/wiring/contract guards all green; `make contract-breaking` reports no breaking change.
- [ ] E5.3 SLA/SLO tracking on work items (business hours, pause-on-hold, breach escalation)  ← deferred again; founder chose a HARDENING pass next (2026-08-06, see E-HARD)
- [ ] E5.4 Problem management (root cause, known errors) linked to incidents/CIs
- [ ] E5.5 Change management (change request → CAB approval via approval quorum, change calendar, conflict/collision detection, risk)
- [ ] E5.6 Major-incident handling, timeline, post-incident review
- **Edge cases:** timezones/business-hours, SLA pause semantics, escalation loops, linked/duplicate incidents, reassignment races.

### E6 — Service Catalog & Request Management  `[ ]`
- [ ] E6.1 Catalog (offerings, request items, forms)
- [ ] E6.2 Request → approval (quorum) → fulfillment (workflow) → asset provisioning link
- **Edge cases:** multi-step approvals, request SLAs, catalog per-tenant/per-role visibility.

### E7 — NOC Command Center  `[x]` — **FOUNDER CHOSE THIS NEXT (2026-08-05).** Read PROJECTIONS over the primitives (NOT reified Dashboard/Report nouns — Vol III §3.4); precedent = graph-traversal / on-call / asset-health / policy-run endpoints (all computed-at-read-time). Polling v1; real-time streaming = E11 (deferred, D2 open).
- [x] E7.1 **NOC operational-overview projection.** One `GET /v1/admin/noc/overview` endpoint, one RLS-scoped appPool read (RLS does tenant isolation — no explicit predicate, no privileged pool), `PermAdmin`, transient DTO, bounded/indexed COUNT+small-list queries. Aggregates the whole loop's CURRENT state: incidents (open total, by_status, by_severity, root-grouping summary via `incident.root_incident_id`), firing alerts (count + by severity — new `AlertRuleRepository.CountFiring` store query, not list-all-in-memory), asset health (reuse `AssetRepository.Health()`), on-call now (list active schedules, capped at 100 + documented, + `OnCall(now)` each), active escalations (new `EscalationStateStore.CountActive`, its first tenant-scoped read role over `escalation_state` — table exists since E5.2b-2). Telemetry summary DEFERRED to keep it bounded (ADR-NOC-001). Additive openapi (contract bijection; `make contract-breaking` clean). No new tables/entities — census unchanged. Two/three-tenant RLS isolation + empty-tenant + aggregate-correctness all mutation-proven against real Postgres. **Merged to master `a6fb5ac` (CTO fast self-review: RLS isolation via appPool verified, 3-tenant test bites).**
- [x] E7.2 **Live NOC screen.** A self-contained vanilla HTML/CSS/JS page (`internal/httpapi/nocweb/noc.html`, no build step, no React/`webdist/` change), embedded and served unconditionally at `GET /noc` (outside `/v1`, alongside `/`/`/docs` in the contract-bijection scope). Polls `GET /v1/admin/noc/overview` on load + every 15s (`setTimeout`-scheduled, no overlap), renders incidents/alerts/assets/on-call/escalations cards with severity/status color coding, a last-updated timestamp, manual refresh, and an error banner (401/403 → "not authenticated", 5xx → retryable). Auth: dev mode (`ONEOPS_AUTH_ENABLED=false`) works with zero configuration (synthetic platform-admin claims already satisfy `PermAdmin`); when auth is enabled, a minimal paste-token-to-`sessionStorage` fallback carries a bearer token, since the console's in-memory-only OIDC session cannot transfer across a separate page load (ADR-NOC-002). No new table/entity/migration — pure read-only projection renderer. **Merged to master `4140b42` (CTO ran it + visually verified the rendered board in a browser).**
**⭑ UI DESIGN-SYSTEM DECISION (founder-driven 2026-08-06): adopt AWS Cloudscape as the console design system.** Founder judged the hand-rolled HTML/CSS (E7.2/E7.3a-vanilla) as dated and directed a modern, responsive, designer-approved, industry-standard cloud design system. CTO chose **AWS Cloudscape** (`@cloudscape-design/components` + `global-styles` + `board-components`) — the open-source ops-console design system behind the AWS Console: React, WCAG 2.1 AA, responsive, dark-mode, Apache-2.0, bundles into `webdist` via Vite (self-contained, no runtime CDN). Rationale: ops-console-native components (AppLayout shell, Board widgets, Table+PropertyFilter+expandable rows, SplitPanel, StatusIndicator) that a generic lib would force us to reinvent; "designer-approved" without a designer on staff; theming API to move toward OneOps brand later. Existing `web/src/api.ts`+`auth.ts` fully reused; the vanilla `/noc` + `nocweb/noc.html` are a STOPGAP to be retired once the Cloudscape NOC screen exists. **STACK: React + Vite + Cloudscape (Next.js considered + DECLINED by founder 2026-08-06 — keep the single-binary go:embed deploy; Cloudscape is client-rendered so Next SSR adds nothing here).** Frontend gate: `make web-test` (tsc + vitest) — wire it into the review loop for every UI story.
  - [x] **E7-UI.0 Console design-system FOUNDATION** — installed Cloudscape (`@cloudscape-design/components`+`global-styles`+`board-components`) + `react-router-dom` v6 into `web/`; built the `Shell` (`AppLayout` + `TopNavigation` + `SideNavigation` + `BreadcrumbGroup`, dark-mode toggle following the OS with a `sessionStorage`-persisted override) as the home for ALL sections (Estate/Governance live, NOC/Incidents placeholders routing to a `ComingSoon` screen); migrated the existing Estate/Governance/Audit screen onto it with zero behavior regression (Ratify flow incl. idempotency-key reuse + full status-code handling, filters round-tripped through the URL, deep-linked artifact detail at `/artifacts/:id`, audit timeline hash-chain badge + expandable chain record, OIDC sign-in/out — all preserved, `api.ts`/`auth.ts`/`governance.ts`/`audit.ts` contracts untouched). `make web` builds self-contained (`webdist` bundle ~965 kB JS/~1,069 kB CSS uncompressed, ~277 kB/~228 kB gzip; grep-verified no runtime CDN reference) and `make web-test` (tsc + 41 vitest tests) is green. ADR-UI-001 records the decision (Cloudscape over a generic lib, Next.js considered+declined, router/URL-shape choices). Branch `feat/e7-ui0-cloudscape-foundation`, not yet merged.
  - [x] E7-UI.1 **NOC overview screen (Cloudscape)** — rebuilt E7.2's board as a Cloudscape KPI-card `Grid` (`Container`+`Header` widgets: Incidents, Alerts firing, Asset health, On call now, Escalations) in the shell over `GET /v1/admin/noc/overview`, reusing `web/src/noc.ts`'s typed client (verified field-for-field against `nocOverviewDTO`). Every count renders through semantic `StatusIndicator` (zero=success, else severity-appropriate type — the same fixed palette ADR-NOC-002 established, carried forward). 15s self-rescheduling poll (no overlap) + manual refresh reusing the same cycle; failure renders through the console's existing `ErrorState`; every widget has an explicit empty/all-clear state. `board-components` considered and declined for this fixed, non-reorderable widget set (reserved for E9). Retired the vanilla `/noc` stopgap: deleted `internal/httpapi/{noc_page.go,noc_page_test.go,nocweb/}`, removed its route + contract-scope comment; `go build`/`make test`/contract bijection all green without it. ADR-NOC-003 records the decision and supersedes ADR-NOC-002. `make web-test` 45/45 (41 pre-existing + 4 new), `make web` clean, `go build ./...` + `make test` green. Branch `feat/e7-ui1-noc-overview`, not yet merged.
  - [x] E7-UI.2 **Incident board (Cloudscape)** — the E7.3a incident board done right: a Cloudscape `Table` with `expandableRows` groups open incidents root→collateral from one bounded fetch (`web/src/incidents.ts`'s `INCIDENT_LIST_CAP = 100`, client-side via `web/src/incidentGrouping.ts` — a `GET /v1/admin/noc/incident-board` projection was considered and declined, see ADR-NOC-004), default-expanded, sorted severity-then-age with a status `Select` filter (default `open`) and client-side `Pagination`; row click opens detail + timeline in the shell's `SplitPanel` (`web/src/components/IncidentDetail.tsx`), driven through a new `Shell`-owned `ShellSplitPanelContext` (`web/src/Shell.tsx`) any future screen can reuse. Reuses the three existing incident endpoints unchanged, verified field-for-field against `incidentDTO`/`incidentEventDTO`; retired the `ComingSoon` placeholder (deleted — both nav entries it existed for are now live). No Go code touched; `go build`/`make test`/contract bijection unaffected. ADR-NOC-004 records the grouping/pagination/split-panel decisions and the honest bound on the client-side grouping. `make web-test` 50/50 (45 pre-existing + 5 new), `make web` clean, no runtime CDN reference. Branch `feat/e7-ui2-incident-board`, not yet merged.
  - [x] E7.3c **Operational boards: alerts board + on-call board** — Cloudscape views mirroring the incident-board pattern (E7-UI.2) over EXISTING endpoints: alerts board (`web/src/routes/AlertsBoardPage.tsx`) is a `Table` over `GET /v1/admin/alert-rules` (rule identified by asset/metric, severity, state, symptom class, threshold, enabled — filterable by state/severity, sortable, paginated, drill-down via `GET /v1/admin/alert-rules/{id}` in the shell's `SplitPanel`); on-call board (`web/src/routes/OnCallBoardPage.tsx`) is `Cards` over `GET /v1/admin/on-call-schedules` (active schedules only) with a bounded (`ON_CALL_DETAIL_FETCH_CAP=20`) per-schedule fetch of `/on-call` (current) + `/participants` (ordered roster), degrading a supplementary-fetch failure without blanking the card. No "linked incident" column: `current_incident_id` is not on the existing `alertRuleDTO` HTTP contract (only on the Go domain model) — an honest, named gap, not a silent one, see ADR-NOC-005. Read-only, no Go touched; `go build`/`make test`/contract bijection unaffected. `make web-test` 61/61 (50 pre-existing + 11 new), `make web` clean (~1,047 kB JS/~297 kB gzip, ~1,128 kB CSS/~234 kB gzip), no runtime CDN reference. Branch `feat/e7-3c-ops-boards`, not yet merged.
  - [x] E7.3b **CMDB topology map** — **SPLIT (CTO, 2026-08-06):** no whole-tenant asset-graph HTTP endpoint exists (only per-asset relationship/dependency queries), so this is a Go-endpoint story + a viz story.
    - [x] E7.3b-1 **Asset-graph projection endpoint** — `GET /v1/admin/assets/graph` → `{nodes:[{asset_id,name,type,status,environment,criticality}], edges:[{from_asset_id,to_asset_id,type}]}`, tenant-scoped via RLS appPool (both `asset` + `asset_relationship` are RLS-owned — one scoped read, no explicit predicate, no privileged pool), `PermAdmin`, additive openapi, BOUNDED (cap nodes 2000 / edges 5000, signal truncation). Read-only projection, no new entity. Two-tenant isolation integration test that BITES (ADR-NOC-006).
    - [x] E7.3b-2 **Topology map UI** — a `/topology` Cloudscape screen + nav item (`web/src/routes/TopologyPage.tsx`) over `GET /v1/admin/assets/graph`, with a hand-rolled, self-contained SVG (`web/src/components/TopologyGraph.tsx`, `web/src/topologyLayout.ts` — no viz library, no CDN, verified by re-checking `make web`'s existing no-runtime-URL grep). Deterministic layered layout ranked by a DFS longest-path over dependency edges (`from_asset_id` depends on `to_asset_id`), top layer = nothing depends on it, descending toward leaf dependencies; **cycle-tolerant** by a white/gray/black DFS colouring — a back edge (including a self-loop) is detected, drawn dashed, and never recursed into or ranked, so any relationship cycle terminates in `O(nodes+edges)` instead of hanging (proven in `topologyLayout.test.ts` and end-to-end in `routes/TopologyPage.test.tsx`). Nodes colored by an incident/health OVERLAY merged client-side (`web/src/topologyOverlay.ts`) from `GET /v1/admin/incidents` (open-class only, `isOpenIncidentStatus`, added to `incidentPresentation.ts`) + `GET /v1/admin/assets/health` by `asset_id` — open incident (red) outranks a health issue (amber); either source degrades independently without blanking the graph. Pan (background drag) + zoom (Cloudscape icon buttons + a native non-passive wheel handler) via SVG `viewBox`, no transform library. Click node → the shell's existing `SplitPanel` (`ShellSplitPanelContext`) with full node detail. `graph.truncated` → Cloudscape `Alert`; empty tenant → clean empty state naming the two seed calls. Palette is a fixed, hand-written light/dark pair (`web/src/topologyPresentation.ts`) tracking `document.body`'s `awsui-dark-mode` class live — `@cloudscape-design/design-tokens` is not installed and was deliberately not added for one screen's colors (ADR-NOC-007). No Go touched; `go build`/`make test`/contract bijection unaffected. `make web-test` 84/84 (61 pre-existing + 23 new: 8 `TopologyPage.test.tsx` + 8 `topologyLayout.test.ts` + 7 `topologyOverlay.test.ts`), `make web` clean (~1,057 kB JS/~300 kB gzip, ~1,128 kB CSS/~234 kB gzip), no new runtime CDN reference. ADR-NOC-007 records the hand-rolled-SVG, layered-layout, cycle-handling, overlay-merge and palette decisions. Branch `feat/e7-3b2-topology-ui`, not yet merged.
- [x] E9 Dashboards (trends over time) — **SPLIT (CTO, 2026-08-06) data-then-viz.** Scout finding: **NO alert-firing history exists** (only `alert_rule.last_state` — alerts are derived, never logged per-firing); so "alert volume over time" is honestly the **alert-*sourced* incident volume** (`incident.source='alert'` from E4.1) — a true firing log would need a new `alert_event` table (deferred to E3 backlog, NOT E9). Telemetry trends REUSE the existing `/v1/admin/telemetry?resolution=rollup_5m`. Charts = Cloudscape-native `LineChart`/`BarChart`/`AreaChart` (already bundled, no new dep).
  - [x] E9.1 **Incident-trends projection** (ADR-NOC-008) — `GET /v1/admin/dashboards/incident-trends?from=&to=&bucket=` → opened (and resolved) counts per contiguous, zero-filled time bucket, split by severity and by source (manual|alert; the alert-volume proxy). Bucketed via `date_trunc($bucket, <col> AT TIME ZONE 'UTC') AT TIME ZONE 'UTC'` over `incident.created_at` (opened) / `resolved_at` (resolved), forcing UTC bucket boundaries regardless of session timezone; ADDITIVE index `ix_incident_tenant_created_at (tenant_id, created_at DESC)` (migration + rollback). Transient DTO (`domain.IncidentTrendsQuery`/`IncidentOpenedTrendPoint`/`IncidentResolvedTrendPoint`), RLS appPool (same `IncidentStore` instance every other incident route uses — no explicit tenant predicate, no privileged pool), `PermAdmin`, bounded to at most 744 contiguous buckets (`MaxIncidentTrendBuckets`, a month of hourly buckets — 422 over cap), additive openapi (`IncidentTrends`/`IncidentTrendPoint`/`IncidentTrendSeverityCounts`/`IncidentTrendSourceCounts`), `make contract-breaking` clean. Two-tenant isolation test that BITES (`TestIncidentTrendsAPI_TenantIsolation_BitesWhenLoosened`, presence-based so it stays robust to reruns against a persistent test database). Honest bound in ADR: `opened_by_source.alert` is the incident-source proxy (E4.1), not a firing log — no `alert_event` table exists. Census bumped to 91 indexes (was 90). Full unit suite (race) 1258 PASS/0 FAIL/6 pre-existing unrelated SKIPs; full integration suite (race, real Postgres) 1669 PASS/0 FAIL/3 pre-existing unrelated SKIPs. Branch `feat/e9-1-incident-trends`, not yet merged/reviewed.
  - [x] E9.2 **Dashboards screen (Cloudscape)** — a "Dashboards" nav item + screen (`web/src/routes/DashboardsPage.tsx`) over the two existing, UNCHANGED reads: E9.1's incident-trends projection (primary chart) and `GET /v1/admin/telemetry?resolution=rollup_5m` (secondary chart). Incident volume is a Cloudscape `MixedLineBarChart` — one stacked bar series per severity (`SEVERITY_CHART_COLOR`, a new hex palette in `incidentPresentation.ts` carrying forward `SEVERITY_TYPE`'s red/red-amber/blue reading) plus a `Resolved` line overlay, `x` a formatted bucket label (categorical, per Cloudscape's own bar-chart guidance); a second `BarChart` renders the alert-volume proxy (`opened_by_source`, honestly labeled "NOT a firing log" per ADR-NOC-008 §5, never merged into the primary legend). A `SegmentedControl` (Last 24h/7d/30d → bucket hour/day) drives a rolling `from`/`to` recomputed every poll tick, refetching correctly (proven by asserting the literal fetch query string before/after switching). Telemetry is a plain `LineChart` over one asset (picked from the existing `GET /v1/admin/assets/graph`, E7.3b-1) + metric (free-text `Input`, default `cpu_utilization` — lifted verbatim from `domain.Sample.Validate`'s own example string); zero samples renders Cloudscape's own `empty` slot cleanly, never a crash or an error. Two independent 15s self-rescheduling poll cycles (trends keyed on the range, telemetry keyed on asset+metric) mirroring `NOCOverviewPage`'s exact cadence/shape, one shared manual `Refresh` button. Nav item + `/dashboards` route added to `Shell`/`App.tsx`. No Go touched (no new endpoint, no migration, no openapi change) — `go build ./...`/`make test`/contract bijection all unaffected by construction. `make web-test` 89/89 (84 pre-existing + 5 new: legend-from-mocked-trends, range-control-refetches-with-right-bucket, empty-telemetry-no-data-state, telemetry-renders-when-present, error-banner-with-retry), `make web` clean (~1,184 kB JS/~344 kB gzip, ~1,164 kB CSS/~238 kB gzip — the ~126 kB/44 kB gzip growth is previously-unused Cloudscape chart modules, now imported), no new runtime CDN reference. ADR-NOC-009 records the chart-type/scale/range-control/proxy-labeling/empty-state/poll decisions. **VISIBILITY EPIC (E7 NOC Command Center + E9 Dashboards) COMPLETE (2026-08-06)** — every screen the 2026-08-05 founder "visibility next" decision asked for is now live: NOC overview, incident/alerts/on-call boards, CMDB topology, and trend dashboards. Branch `feat/e9-2-dashboards-screen`, not yet merged.
- **Edge cases:** per-role/per-tenant views, rollup correctness when data is partial, overview query cost at scale (keep bounded).

### E-ACT — Console operational actions  `[x]` — **FOUNDER CHOSE THIS NEXT (2026-08-06); COMPLETE.** Turn the read-only 7-screen Cloudscape console into one operators WORK in: wire WRITE actions to the EXISTING backend endpoints. New cross-cutting UI concerns (every action honors these): **optimistic-locking `row_version` round-trip** (read carries it, the mutation sends it back, 409 → refetch+retry, surfaced clearly), RFC 7807 error surfacing (422 validation, 409 conflict, 403), **confirmation modals for destructive/irreversible actions**, form validation before submit, and a post-action refetch so the view reflects the new state. Sequenced by daily-operational value.
  - [x] E-ACT.1 **Incident actions** (highest value — the incident board is where operators live): from the incident detail SplitPanel, wired **Acknowledge / transition status** (`POST /v1/admin/incidents/{id}/transition`, sends `row_version`; only legal `CanTransitionTo` moves offered, per-status, proven by test), **Assign** (`POST /{id}/assign`, `assignee_user_id` + `row_version`; Cloudscape `Select` from `GET /v1/admin/users` when reachable, degrading to a free-text user-id `Input` on any directory failure — see the confirmed contract gap below), and **Create incident** (`POST /v1/admin/incidents`, title required) from the board, opening the new incident's detail on success. Consequential transitions (resolve/close) confirm via `Modal` first; every mutation refetches on success AND on 409 (never a blind retry), surfacing RFC 7807 detail inline on any other failure. **CONFIRMED GAP, not fabricated:** no add-note/timeline-append HTTP endpoint exists (`IncidentEventKind`'s own doc comment enumerates the closed write-path list) — deferred, no UI added for it. **CONFIRMED GAP #2:** `GET /v1/admin/users` is `requirePlatformAdmin`-gated, a strictly more privileged tier than the `PermAdmin` the incident endpoints use, so most real tenant operators will see the free-text fallback, not the picker — the local-dev `AuthEnabled=false` identity happens to be platform-admin, which is why the picker works out of the box for local verification. ADR-ACT-001 records the write-action pattern (row_version round-trip, legal-transition gating, confirm-then-send, 409-triggers-refetch) every later E-ACT story inherits, plus both gaps above in full. Frontend only — no Go touched. `make web-test` 101/101 (89 pre-existing + 12 new: legal-transitions-only per status, transition sends status+row_version then refetches, consequential-transition confirm-before-send, 409-refetches-with-notice-no-retry, assign-via-picker, assign-via-fallback-input, mutation-error-surfaced, create-disabled-until-titled, create-posts-and-opens, create-validation-error-keeps-dialog-open), `make web` clean (~1,196 kB JS/~347 kB gzip, ~1,173 kB CSS/~238 kB gzip), no new runtime CDN reference, `go build ./...`/`make test` unaffected. Branch `feat/e-act1-incident-actions`, not yet merged.
  - [x] E-ACT.2 **Alert-rule CRUD** — create/edit/enable-disable/delete alert rules from the alerts board and its detail panel, against the three EXISTING endpoints confirmed field-for-field against the Go first: `POST /v1/admin/alert-rules` (asset_id/metric/comparator/threshold/for_duration_seconds required, severity/symptom_class/enabled/flap_dwell_seconds optional with server defaults), `PATCH /v1/admin/alert-rules/{id}` (row_version required, asset_id/metric NOT patchable — fixed at creation; enable/disable is this same PATCH's `enabled` field, no dedicated route), `DELETE /v1/admin/alert-rules/{id}`. **CONFIRMED CONTRACT GAP, not fabricated:** unlike PATCH (and unlike every E-ACT.1 endpoint), DELETE takes **no `row_version` and no body at all** (`domain.AlertRuleRepository.Delete` has no optimistic-lock parameter) — the delete confirm `Modal` and `deleteAlertRule`'s doc comment both name this rather than sending a fabricated version to look consistent with Edit. `components/AlertRuleForm.tsx`'s `AlertRuleConfigFields` is one shared 7-field sub-form (comparator/threshold/for_duration/severity/symptom_class/flap_dwell/enabled) reused by both the create modal (`routes/AlertsBoardPage.tsx`, +asset_id/metric) and the edit modal (`components/AlertRuleDetail.tsx`, read-only asset_id/metric shown instead) — every enum field a `Select` over the real backend value set, so a bad enum is impossible to submit. Client-side validators in `alertRules.ts` mirror `domain.AlertRule.Validate`'s bounds (metric pattern/length, threshold finiteness, for-duration 1–86400, flap-dwell 0–86400) before the server's own 422 is ever hit. Every mutation: disable-while-pending, refetch on success AND on 409 (never blind-retry, proven by test), RFC 7807 detail surfaced inline on any other failure; Delete is the one consequential action (confirm Modal), Edit/Enable-Disable fire immediately. Does NOT add the additive `current_incident_id` DTO field (the E7.3c/ADR-NOC-005 gap) — the brief this story was built against was explicit frontend-only, and that field is a separate, already-documented follow-up, not something CRUD itself needs. Frontend only — no Go touched. `make web-test` 111/111 (101 pre-existing + 10 new: create-disabled-until-required-fields, enum-selects-constrained-to-real-values, create-posts-exact-body-and-opens-new-rule, create-validation-error-keeps-dialog-open, edit-sends-row_version-and-refetches, asset_id/metric-not-editable, 409-on-edit-closes-dialog-refetches-with-notice-no-retry, enable/disable-sends-row_version-then-refetches, delete-confirms-then-sends-no-row_version-then-closes-panel-and-reloads-board, delete-error-surfaced-without-removing-the-rule), `make web` clean (~1,209 kB JS/~350 kB gzip, ~1,177 kB CSS/~239 kB gzip), no new runtime CDN reference, `go build ./...`/`make test`/`make lint` unaffected. ADR-ACT-002 records the pattern reuse, the shared-sub-form decision, and the delete-has-no-row_version contract fact in full. Branch `feat/e-act2-alert-rule-crud`, not yet merged.
  - [x] E-ACT.0 **Tenant-scoped user-list endpoint (Go, the shared dependency; founder-directed 2026-08-06)** — the existing `GET /v1/admin/users` is `requirePlatformAdmin` (cross-tenant), unreachable by a tenant operator, so E-ACT.1's Assign degrades to free-text and E-ACT.4's roster picker would too. Added a read-only projection `GET /v1/admin/tenant-users` → `{items:[{user_id, email, display_name}]}` for **ACTIVE members of the CURRENT tenant**: `MembershipStore.ListActiveDirectory` joins RLS-scoped `membership` (current tenant, `status='active'`) INNER JOINed to global `app_user` (same join-safety argument `OnCallScheduleRepository.OnCall` already established for display_name), filtered ADDITIONALLY on `app_user.status='active'` — a deliberate, documented implementation call (a suspended platform account must not populate a picker even if its tenant membership was never individually revoked; recorded, not silently assumed, in ADR-ACT-003 §4 for review). `requirePermission(auth.PermAdmin)` (NOT platform-admin — the same tier `GET /admin/memberships` already uses, since `membership` is tenant-owned/RLS), narrow `tenantUserDirectory` interface (mirrors `nocEscalationReader`) rather than widening `domain.MembershipRepository`, transient `domain.TenantUserSummary` DTO (no new table — census unchanged), keyset-paginated over `user_id` (`clampMembershipPage`, 50/500, same bound `ListByOrg` uses). Two-tenant isolation test that BITES (`TestTenantUsersAPI_TenantIsolation` + its mutation counter-proof `..._BitesWhenLoosened`, which rewires `MembershipStore` over the privileged pool and requires the leak to occur there); correctness test seeds N active members + a suspended-account member + a revoked-membership member in the SAME tenant and asserts exactly the N active ones return (`TestTenantUsersAPI_ReturnsExactlyActiveMembers`); empty-tenant clean `{"items":[]}` (`TestTenantUsersAPI_EmptyTenantIsCleanEmpty`); keyset pagination walk (`TestTenantUsersAPI_Pagination`). Additive openapi (`TenantUser`/`TenantUserList`), `make contract-breaking` clean; both privileged arch guards green (`MembershipStore` has no privileged-pool instance anywhere in this codebase, so it adds no candidate to `TestPrivilegedReads_AreScopedToATenant`'s sweep; `SetTenantUserDirectory` reuses the same appPool-scoped `membershipStore` variable `SetMemberships` already does, so `TestServerWiringUsesTenantScopedPool` needs no new exemption). ADR-ACT-003 records the decision in full, including the `GET /admin/users` vs this route split and the app_user-status filter judgment call. Full unit suite (race) 1263 PASS/0 FAIL; full integration suite (race, real Postgres) 1679 PASS/0 FAIL/3 pre-existing unrelated SKIPs, including 9 new tenant-user tests. **Frontend rewire of the E-ACT.1 assignee picker DEFERRED to E-ACT.4** (this story's deliverable is the shared Go endpoint both later stories depend on, not the frontend consumer) — `web/src/users.ts` and `IncidentDetail.tsx` are UNCHANGED by this story. `go build ./...`/`make lint` clean, no migration (no new table). Branch `feat/e-act0-tenant-users`, not yet merged.
  - [x] E-ACT.3 **Maintenance-window create/cancel** — a new top-level "Maintenance" nav item/`/maintenance` screen (not the alerts/asset context the brief first proposed — neither is a reachable seam today; ADR-ACT-004 §2 records why), against the four EXISTING endpoints confirmed field-for-field against the Go first: `GET /v1/admin/maintenance-windows` (keyset-paginated, `{items:[...]}`, no cursor in the body — one bounded page, `MAINTENANCE_WINDOW_LIST_CAP=100`), `GET .../{id}` (confirmed, unused — the list already returns the full DTO and this story has no drill-down surface), `POST /v1/admin/maintenance-windows` (asset_id/starts_at/ends_at required RFC3339, reason optional ≤500 chars; `domain.MaintenanceWindow.Validate` rejects `ends_at <= starts_at` — the window is half-open `[starts_at, ends_at)`, ADR-ALERTING-002 §3), `DELETE .../{id}`. **CONFIRMED CONTRACT GAP, not fabricated:** the identical asymmetry ADR-ACT-002 found for `deleteAlertRule` — DELETE takes **no `row_version` and no body at all** (`domain.MaintenanceWindowRepository.Delete` has no optimistic-lock parameter) — the cancel confirm `Modal` and `deleteMaintenanceWindow`'s doc comment both name this. A `Table` (asset/window-range/reason/status/suppressed-count/actions columns) computes active-now/upcoming/expired **client-side** from `now` against each window's own bounds (no such field exists on the DTO) via `computeMaintenanceWindowStatus`. "Schedule maintenance" is a `Modal` with free-text asset_id (no picker exists anywhere in this console — same precedent as alert_rule's own asset_id), a `DateInput`+`TimeInput` pair per bound (combined browser-locally, sent as UTC RFC3339), and an optional reason; client-side validators in `maintenanceWindows.ts` mirror `Validate`'s bounds (the half-open rule specifically, not just "end < start") before the server's own 422 is ever hit. No edit exists — `MaintenanceWindowRepository` has no `Update`/`Patch` method; a window is created or cancelled, nothing in between. Every mutation: disable-while-pending, refetch on success, RFC 7807 detail surfaced inline on any other failure; Cancel is the one consequential action (confirm Modal), Schedule fires on submit. Frontend only — no Go touched. `make web-test` 120/120 (111 pre-existing + 9 new: list-renders-with-status-computed-from-a-fixed-now, empty-state, error-and-retry, create-disabled-until-asset-id-and-valid-range, create-posts-exact-RFC3339-UTC-body-and-refetches, create-blocked-client-side-on-non-strictly-after-end-including-equal-bounds, create-validation-error-keeps-dialog-open, cancel-confirms-then-sends-no-row_version-then-refetches, cancel-error-surfaced-without-removing-the-row), `make web` clean (~1,254 kB JS/~363 kB gzip, ~1,177 kB CSS/~239 kB gzip), no new runtime CDN reference, `go build ./...`/`make test` unaffected. ADR-ACT-004 records the pattern reuse, the no-drill-down/no-picker/no-edit decisions, and the delete-has-no-row_version contract fact in full. Branch `feat/e-act3-maintenance-windows`, not yet merged.
  - [x] E-ACT.4 **On-call schedule + roster management** — create/edit a schedule and manage its participant roster (add/remove/reorder) from the on-call board, against the SIX EXISTING E5.2a endpoints confirmed field-for-field against the Go first: `POST /v1/admin/on-call-schedules` (name/handoff_interval_seconds/rotation_start_at required), `PATCH .../{id}` (row_version required; name/handoff_interval_seconds/rotation_start_at/status all independently optional), `POST .../{id}/participants` (user_id only, appends at the end — 404 if not an active tenant member, 409 if already rostered), `DELETE .../participants/{participantId}`, `POST .../participants/reorder` (the schedule's FULL current participant-id set in the new order, atomic). **CONFIRMED CONTRACT FACT:** unlike schedule PATCH, **none** of add/remove/reorder-participant carry a `row_version` at all (no optimistic-lock parameter on any of `AddParticipant`/`RemoveParticipant`/`ReorderParticipants`) — recorded, not fabricated. **CONFIRMED SCOPE DECISION:** `DELETE .../{id}` (schedule delete) exists but is deliberately NOT wired — `domain.OnCallSchedule`'s own doc comment says a schedule "is a governed, named object an operator archives rather than deletes," so Edit's `status` field (`active`/`archived`) is the only retirement path; no `deleteOnCallSchedule` function exists. Roster management opens in the shell's `SplitPanel` from the (still `Cards`-based) board via a new "Manage schedule" action; the add-participant picker is a Cloudscape `Select` backed by `GET /v1/admin/tenant-users` (E-ACT.0/ADR-ACT-003) filtered to exclude users already on the roster, degrading to free-text on any directory failure. Reorder always sends the schedule's full ordered participant-id set in one call (never partial/interleaved moves) via up/down icon buttons. **This story also rewires E-ACT.1's incident assignee picker** (`web/src/users.ts`, `components/IncidentDetail.tsx`) from the platform-admin-gated `GET /v1/admin/users` onto `GET /v1/admin/tenant-users` — closing the ADR-ACT-001 §2 / ADR-ACT-003 confirmed gap so a real `PermAdmin`-holding tenant operator, not just the local-dev platform-admin identity, gets a working `Select` rather than the free-text fallback. Every mutation: disable-while-pending, refetch on success, RFC 7807 detail surfaced inline; Edit's 409 uses the ADR-ACT-001 reload-and-notice path (a genuine stale-`row_version` conflict); add-participant's 409 (a business-rule "already rostered" conflict, not a stale read) is surfaced as an ordinary inline error instead — a deliberate, documented split, not an inconsistency. **Documented minor gap:** the `SplitPanel` header is set once at open time and does not follow a rename (the panel body's own `Name` field and the board's card title both do, via refetch). Frontend only — no Go touched. `make web-test` 128/128 (120 pre-existing + 8 new: create-disabled-until-answered, create-posts-exact-body-with-resolved-preset-and-opens, create-validation-error-keeps-dialog-open, edit-sends-row_version-then-refetches, edit-409-refetches-with-notice-no-retry, add-participant-uses-tenant-users-picker-excluding-rostered-and-posts-user_id, reorder-sends-full-ordered-set, remove-confirms-then-deletes-and-refetches), `make web` clean (~1,266 kB JS/~366 kB gzip, ~1,177 kB CSS/~239 kB gzip), no new runtime CDN reference, `go build ./...`/`make test`/`make lint` unaffected. ADR-ACT-005 records the pattern reuse, the schedule-archives-not-deletes decision, the two-kinds-of-409 split, and the tenant-users-picker adoption (both here and in the E-ACT.1 rewire) in full. Branch `feat/e-act4-oncall-management`, not yet merged.
  - [x] E-ACT.5 **Escalation policy + tier management** — create/edit a policy and manage its ordered tier ladder (add/remove/reorder) from a new "Escalation" nav item/`/escalation` screen, against the SEVEN EXISTING E5.2b-1 endpoints confirmed field-for-field against the Go first: `POST /v1/admin/escalation-policies` (name only, required), `PATCH .../{id}` (row_version required; name/status both independently optional), `DELETE .../{id}` (exists, deliberately NOT wired — see below), `POST .../{id}/tiers` (on_call_schedule_id/wait_seconds, appends at the end — 404 if on_call_schedule_id does not name a schedule of this tenant or the policy itself does not exist, 409 if the tier "could not be added"), `DELETE .../tiers/{tierId}`, `POST .../tiers/reorder` (the policy's FULL current tier-id set in the new order, atomic — the same `uq_escalation_tier_policy_position` DEFERRABLE INITIALLY DEFERRED pattern E5.2b-1 reused from E5.2a). **CONFIRMED CONTRACT FACT:** unlike policy PATCH, **none** of add/remove/reorder-tier carry a `row_version` at all (no optimistic-lock parameter on any of `AddTier`/`RemoveTier`/`ReorderTiers`) — the identical asymmetry ADR-ACT-005 found for on-call participants. **CONFIRMED SCOPE DECISION:** `DELETE .../{id}` (policy delete) exists but is deliberately NOT wired — `domain.EscalationPolicyStatus`'s own doc comment applies the identical archive-not-delete doctrine `OnCallSchedule` uses ("a policy is a governed, named object an operator archives rather than deletes"), so Edit's `status` field (`active`/`archived`) is the only retirement path; no `deleteEscalationPolicy` function exists, mirroring ADR-ACT-005 Decision 1 exactly. **CONFIRMED SCHEMA FACT (checked against the migration, not assumed):** `escalation_tier` has NO uniqueness constraint on `on_call_schedule_id` (only on `(tenant_id, policy_id, position)`) — a policy MAY legitimately page the same on-call schedule at two different tiers, so unlike E-ACT.4's roster picker, the add-tier schedule `Select` (backed by `GET /v1/admin/on-call-schedules`, active schedules only) does NOT exclude schedules already used by another tier of this policy. Tier management opens in the shell's `SplitPanel` from a `Table`-based board (chosen over `Cards` — a policy's own board fields are simple name/status columns, the same reasoning `AlertsBoardPage` used) via a "Manage tiers" action; reorder always sends the policy's full ordered tier-id set in one call (never partial/interleaved moves) via up/down icon buttons; remove confirms first. Every mutation: disable-while-pending, refetch on success, RFC 7807 detail surfaced inline; Edit's 409 uses the ADR-ACT-001 reload-and-notice path (a genuine stale-`row_version` conflict); add-tier's 409 (a business-rule conflict, not a stale read) is surfaced as an ordinary inline error instead — the identical split ADR-ACT-005 Decision 3 made for add-participant. Frontend only — no Go touched. `make web-test` 140/140 (128 pre-existing + 12 new: policy-list-renders, empty-state, error-and-retry, create-disabled-until-named, create-posts-exact-body-and-opens, create-validation-error-keeps-dialog-open, edit-sends-row_version-then-refetches, edit-409-refetches-with-notice-no-retry, add-tier-uses-schedule-picker-and-posts-on_call_schedule_id+wait_seconds, reorder-sends-full-ordered-tier-set, remove-confirms-then-deletes-and-refetches, add-tier-error-surfaced-as-inline-business-conflict), `make web` clean (~1,280 kB JS/~369 kB gzip, ~1,177 kB CSS/~239 kB gzip), no new runtime CDN reference, `go build ./...`/`make test`/`make lint` unaffected. **A pre-existing, unrelated test-suite flake was OBSERVED (not introduced): under the full 17-file suite's heavier parallel load, `audit.test.tsx`/`IncidentDetail.test.tsx` (both untouched by this story) intermittently miss a default `waitFor` timeout (~1-in-3 full-suite runs); the SAME flake is absent from a 16-file run of `master` alone across 3 consecutive runs, and this story's own 12 new tests never fail in any run (isolated or full-suite) — a resource-contention timing issue in existing tests, not a logic defect this story introduced or should fix under its frontend-only scope.** ADR-ACT-006 records the pattern reuse, the archive-not-delete/no-duplicate-schedule-exclusion decisions, and the confirmed contract facts in full. Branch `feat/e-act5-escalation-management`, not yet merged.
  - **Edge cases:** concurrent edits (409/row_version), a transition made illegal by someone else's change between read and write, partial-failure of a multi-step form, permission (403) — surface, never silently swallow.

**E-ACT EPIC COMPLETE (2026-08-06).** All five console operational-action stories (E-ACT.0–E-ACT.5) are landed: incident actions, alert-rule CRUD, maintenance-window create/cancel, on-call schedule + roster management, and escalation policy + tier management — every write action the read-only Cloudscape console needed to become an operator's daily-work surface, each against EXISTING backend endpoints confirmed field-for-field against the Go before any UI was written, with row_version optimistic-locking, RFC 7807 error surfacing, and confirm-before-destructive-action applied consistently (ADR-ACT-001 through ADR-ACT-006). None of the six branches are merged to master yet — that is the next gating step, not a new story.

### E-HARD — Hardening & polish  `[x] COMPLETE (2026-08-06)` — **FOUNDER CHOSE THIS after E-ACT completed.** Paid down accumulated debt before more surface. **All 7 items done + merged to master** (git log authoritative): E-HARD.1 test determinism, .2 tenant-users filter, .3 linked-incident column (`04b6d4a`), .4 code-splitting (`cdf5a41`, ADR-UI-002), .5 delete-row_version DECIDED accept+document (`1c8fd0f`, ADR-HARD-003), .6 CI self-contained-bundle check (`76cf6ee`), .7 TopologyPage staleness. Each CTO-gated (diff review + independent re-run of the relevant gate). No `▶ CURRENT` front is active — **awaiting founder direction** (finish deeper backend epics, or checkpoint / pivot to customer evidence per the standing charter). (E-ACT branches also all merged: `9808feb`,`aa78072`,`af52f90`,`9b50acf`,`b76b1ae`,`7d69dec`.)
- [x] E-HARD.1 **Frontend test-suite determinism** — `make web-test` (140 tests / 17 files) intermittently failed (reproduced: ~4 failures across ~60 consecutive full-suite runs, always the same assertion) — an async-race shape confirmed live: `AuditTimeline`/`IncidentDetailPanel`/`NOCOverviewPage` each fetch in more than one independently-resolving step, and one test per component awaited a static or already-resolved element then synchronously `getBy*`-asserted a DIFFERENT, still-in-flight data-dependent one. Fixed at the four confirmed race sites (five assertions in `audit.test.tsx`, one in `routes/IncidentBoardPage.test.tsx`, one in `routes/NOCOverviewPage.test.tsx`) by converting the specific racing assertion to `await findBy*`/`findAllBy*` — real awaits, no timeout widening, no vitest-config change needed. Audited all 17 test files for the same shape. Proof: 20 consecutive full-suite runs, 0 failures. ADR-HARD-001. Noticed-but-out-of-scope: `TopologyPage.tsx`'s `selectNode` captures its incident/health overlay into a split-panel `ReactNode` at click time from two more independently-resolving fetches — a potential real staleness bug (not a test flake; did not reproduce in this story's runs) flagged for a future story, not fixed here (frontend/test-only scope). Branch `feat/e-hard1-test-determinism`, not yet merged.
- [x] E-HARD.2 **Tenant-users filter** — relaxed `MembershipStore.ListActiveDirectory`'s `app_user.status` predicate from `= 'active'` to `IN ('active', 'invited')` (ADR-ACT-003 §4 amendment, 2026-08-06): an ACTIVE member now appears in the directory regardless of whether their global account is `active` or still `invited` (onboarding not finished), while `suspended`/`deactivated` accounts remain excluded — the assign/roster picker was empty during onboarding otherwise. `membership.status = 'active'` unchanged; RLS-scoped `appPool` read unchanged; no new table/migration. `TestTenantUsersAPI_ReturnsExactlyActiveMembers` extended to mutation-verify both directions live (invited-but-active-member included; suspended AND deactivated accounts excluded — reverting the predicate to `= 'active'` makes the new assertion fail, confirmed live). Two-tenant isolation test (`TestTenantUsersAPI_TenantIsolation`) and its mutation canary (`..._BitesWhenLoosened`) unchanged and still pass/still bite. Full unit suite (race) green; full integration suite (race, real Postgres) — httpapi+postgres packages alone: 1001 PASS/0 FAIL/2 pre-existing unrelated SKIPs, whole-repo run clean; both privileged arch guards (`TestPrivilegedReads_AreScopedToATenant`, `TestServerWiringUsesTenantScopedPool`) green; contract bijection green, `make contract-breaking` clean (no openapi.yaml change — query-only story); `make lint` clean. Branch `feat/e-hard2-tenant-users-filter`, not yet merged.
- [x] E-HARD.3 **`current_incident_id` on alertRuleDTO** (Go, small additive) — DONE: field (already on the domain model + store scan) now serialized on `alertRuleDTO` with `omitempty` + documented in openapi (nullable, not required — additive, non-breaking); alerts board gained a "Linked incident" column that click-throughs to the incident via the existing SplitPanel idiom (`IncidentDetailPanel`, no new route), em-dash when null. Closes the ADR-NOC-005 gap. Contract bijection tests non-vacuous PASS; web-test 143/143; the new frontend test clicks the link and asserts the incident panel loads real data. Stale AlertRuleDetail contract-comment corrected in the same change.
- [x] E-HARD.4 **Bundle code-splitting** (frontend) — DONE (ADR-UI-002): route pages `React.lazy`-loaded behind an Outlet-level `<Suspense>` (nav stays visible), + Cloudscape pinned to its own `manualChunks` vendor chunk. Main entry **1.28MB → 32KB** (369KB → 11.85KB gzip); each route now a 5–16KB on-navigation chunk; Cloudscape isolated at ~1.14MB (327KB gzip), `modulepreload`-ed once then cached across app-code deploys. The lone remaining >500KB warning is the Cloudscape library chunk itself — **intentional, do NOT silence** (see ADR-UI-002). Gate: web-test 143/143; go:embed builds; self-contained grep PASS; only 3 src files touched.
- [x] E-HARD.5 **Delete-without-row_version** — DECIDED: **accept + document** (ADR-HARD-003), not build. DELETE is idempotent-on-identity, so optimistic-lock on delete is a confirm-UX nicety, not a correctness invariant (destructive deletes already confirm). The one safety-bearing case — paging a removed on-call user — is closed at PAGE-TIME (`escalation/worker.go` `ActiveMember` re-check, ADR-ONCALL-003 §4), independent of delete-time locking; referential integrity is an FK concern, orthogonal to row_version. Closing it is a breaking contract change (or false-confidence optional param) for marginal value on admin-only low-contention deletes. Reopens only on a concrete observed lost-work incident. No code change.
- [x] E-HARD.6 **Frontend gate in CI** — CORRECTION: CI already had a `web` job (pnpm install + tsc typecheck + `vitest run` + build bundle, `.github/workflows/ci.yml`), so the frontend TEST gate was NOT missing — and post-E-HARD.1 it's reliable on the clean runner. The real gap was NO automated **self-contained-bundle check**: added a CI step that greps the built `webdist/assets` for external hosts and FAILS on any beyond the documented-inert allowlist (w3.org/reactjs.org/react.dev/fonts.google.com/github.com — none fetched at runtime), enforcing the `go:embed` no-runtime-CDN invariant (ADR-UI-001) that was only ever checked by hand. Validated against the current bundle (passes; catches a new CDN).
- [x] E-HARD.7 **TopologyPage split-panel staleness** (frontend, REAL bug found by E-HARD.1/ADR-HARD-001) — `web/src/routes/TopologyPage.tsx` `selectNode` materialized the incident/health overlay into an opaque `ReactNode` at CLICK time and handed it to `Shell.openSplitPanel`; if the two overlay fetches (`listIncidents`/`getAssetHealth`) resolved AFTER the click, the node detail showed stale/incomplete overlay and never refreshed without a re-click. Fixed with a `TopologyPage`-local `useEffect` keyed on `[overlay, overlayLoading]` that re-`openSplitPanel`s the still-selected node's content once the overlay changes, guarded by a new additive `isSplitPanelOpen` field on `ShellSplitPanelContext` so a panel the user closed is never reopened; an explicit "Checking…" loading state (new `overlayLoading` prop on `TopologyNodeDetail`) replaces the ambiguous "None" shown before the overlay resolves. New `TopologyPage.test.tsx` test proves it: holds the incidents fetch open, clicks before it resolves, asserts no false "None"/resolved claim, resolves it, asserts the panel updates with no re-click — confirmed failing against the pre-fix code, passing after. `make web-test` 17/17 files, 141/141 tests; the other four `ShellSplitPanelContext` consumers (incident/alert/on-call/escalation boards) re-run explicitly, unaffected (43/43 tests). `go build`/`make test` untouched (frontend-only). ADR-HARD-002. Branch `feat/e-hard7-topology-panel-staleness`, not yet merged.

### E-ID — Identity & Access console  `[x] COMPLETE (2026-08-07)` — **FOUNDER DIRECTIVE (2026-08-06)**: "add login register role permissions ui integration". CTO challenged + founder decided **RELYING-PARTY** model (not app-owned identity): OneOps keeps OIDC (IdP owns login + roles), and the console SURFACES + MANAGES the existing identity backend. Scouted reality (`oneops-scout`, 2026-08-06): auth = OIDC Auth-Code+PKCE, no password path (ADR-IDENTITY-003); roles are a JWT `roles` claim, NOT a DB column — 4 roles (`oneops-reader/editor/admin/platform-admin`) map exact→ permissions (`artifacts:read/write/delete/admin` tenant-scoped, `platform:admin` platform-scoped) in `internal/auth/rbac.go`; registration is invite-only (ADR-IDENTITY-002); invitation domain+store EXIST but have NO HTTP endpoints; users/orgs/memberships endpoints exist; **zero identity UI in the console today**. Two things are DELIBERATELY EXCLUDED as architecture-contradicting (would need an ADR + security decision, not a UI story): in-app password login/self-signup (vs OIDC) and editable per-member roles (roles are IdP-owned — OneOps can show them, not mutate them).
- [x] E-ID.1 **Administration section + identity + read-only Roles×Permissions matrix** — DONE (ADR-IAC-001): new "Administration" nav entry; "Who am I" panel (subject + roles via a new display-only `getRoles` decoding the JWT `roles` claim, effective permissions from a restated `rbac.go` map in new `web/src/rbac.ts`; graceful auth-disabled/no-token notice); read-only 4-role × 5-perm matrix with scope labels + honest "roles are IdP-assigned, not editable here" note. `getRoles`/`rbac.ts` are DISPLAY-ONLY (server still enforces); `rbac.test.ts` pins the mirror to `rbac.go`. Gate: web-test 156/156 (13 new); rbac.ts diffed = rbac.go exactly; self-contained grep clean; **CTO visual gate PASS — rendered live (auth-disabled) in light AND dark mode**, matrix grants verified against rbac.go.
- [x] E-ID.2 **Members screen** (tenant admin / PermAdmin) — DONE (2 commits): list memberships (names joined from `/admin/tenant-users`, degrade-to-id), grant (`POST /admin/memberships`), revoke (`DELETE`, confirm modal, no row_version per ADR-HARD-003), 403→permission state, write-action pattern. **E-ID.2a** added `GET /admin/tenant-org` (PermAdmin, context-only, tenant-isolation-proven — ADR-IAC-002) so the org_id **auto-resolves** — the admin never types a ULID; manual entry remains only as a non-403 fallback. Gate: web-test 164/164; Go shapes mirrored; tenant-org isolation integration test re-run independently (--- PASS, non-vacuous); **CTO visual gate PASS — live: org auto-resolved to "System", 2 members listed with names, grant modal honest, Administration nav is now a section (Identity & roles + Members).**
- [x] E-ID.2a **`GET /admin/tenant-org`** — DONE (`4f51d16`, ADR-IAC-002): PermAdmin endpoint returning the caller's own org (tenant from context ONLY — org is outside RLS so this handler is the sole isolation control; two-tenant isolation test proves it). Enables E-ID.2's org auto-resolution.
- [x] E-ID.3 **Users screen** (platform admin) — DONE: list/create/rename/status-lifecycle over `/admin/users` (all `requirePlatformAdmin`). New `web/src/users_admin.ts` (separate from the tenant-users picker `users.ts`); create (email+display_name), rename + setUserStatus as TWO split patch fns (server refuses both in one PATCH), row_version round-trip + 409→refetch+notice, legal-transition gating restated from `domain/user.go` `userTransitions` (pinned by test), confirm on deactivate (terminal), 403→PermissionNeeded. Extends ADR-IAC-001 (no new ADR). Gate: web-test 181/181; transition map = user.go:45-50; self-contained clean; **CTO visual gate PASS — live (auth-disabled=platform-admin): 2 invited users, legal-transition gating visibly correct (invited shows Activate+Deactivate, NOT Suspend), Administration nav = Identity & roles + Members + Users.**
- [x] E-ID.4a **Invitations ADMIN backend** (create/list/revoke) — DONE (ADR-IAC-003): three PermAdmin endpoints wired to the existing invitation store. Target org/tenant from the caller's CONTEXT only (never the request — smuggled org_id ignored, proven); the one-time plaintext token appears ONLY on the create response, never in a list/get DTO or a log (invitationDTO has no token/token_hash field); TTL server-side (7d); cross-tenant revoke→404. Gate: 5 invitation integration tests --- PASS incl. tenant isolation (re-run independently, non-vacuous); contract bijection PASS; make test (race) green; lint clean; go build/vet clean.
- [x] E-ID.4b **Invitation REDEEM** (UNAUTHENTICATED, security-gated) — DONE (ADR-IAC-004): `POST /auth/invitations/redeem` (top-level, `invariantGate`+`rateLimit`, NO `authenticate`), token-only body, HTTP transport onto the PRE-EXISTING atomic `RedemptionStore.Redeem` (OPS-S026 — consume + find/create user by email + grant active membership + audit as InvitationBearerActor, one tx, rolls back suspended/deactivated). **oneops-security adversarial pass: NO merge-blocking defect** across all 4 critical classes (cross-tenant, enumeration, single-use race, reactivation) + atomicity + invariant-gate + input-abuse — all CLOSED with kept regression guards. **CTO fixed finding #1 (Medium): added `http.MaxBytesReader` 4KiB body cap → 413**, guard test asserts it. 2 accepted residuals (documented in ADR): low-sev timing side-channel (only a valid-token holder), shared anon rate-limit bucket (deferred to ingress per ADR-SECURITY-004). **OPEN: OIDC reconciliation** — redeem provisions the OneOps side; how a later SSO login for the same email links is an IdP-integration story (recorded in ADR-IAC-004). Gate: 13 redeem+adversarial integration tests --- PASS (independently re-run); contract PASS; httpapi+arch race green; lint 0.
- [x] E-ID.5 **Invitations UI** — DONE: (A) admin "Invitations" screen (Administration section) — invite-by-email → one-time token+redeem-link reveal modal (copy buttons + "shown once" warning), list, revoke (confirm), 403-graceful; (B) PUBLIC invitee `/redeem` screen rendering SIGNED-OUT via an App.tsx public-route bypass (before the auth gate, scoped to `/redeem`, skips /auth/config) — reads `?token=`, posts to the unauthenticated `/auth/invitations/redeem`, single generic error, success → "You've joined {org}. Sign in to continue." + OIDC CTA. Gate: web-test 195/195; TS shapes mirror handlers/openapi; self-contained clean; **CTO visual gate PASS — live end-to-end: admin created invite → invitee opened the link signed-out → "Invitation accepted, You've joined System" → sign-in CTA.** No new ADR (governed by ADR-IAC-003/004).
- **✅ E-ID EPIC COMPLETE (2026-08-07)** — "add login register role permissions ui integration" delivered under the relying-party model: login (OIDC, surfaced) · roles/permissions (read-only matrix, IdP-owned) · members mgmt · users mgmt · **register (invitations: admin invite + unauthenticated redeem, security-gated)**. All CTO-gated (diff review + independent gate re-run + live visual). Excluded by design (need an ADR, not a UI story): in-app password login/self-signup, editable per-member roles. ADRs IAC-001..004.
- [ ] E-ID.5 **Invitations UI** — admin invite (create → surface the one-time token/link to share) + list/revoke pending (over E-ID.4a) + an invitee redeem screen (over E-ID.4b).
- [ ] E-ID.5 **Invitations UI** — admin invites a user (create → surface the one-time token/link to share) + an invitee redeem screen (enter token → become active). Completes the register flow.

### E8 — SOC / Security Platform  `[x] COMPLETE (2026-08-07)` — **FOUNDER CHOSE THIS as the new backend epic (2026-08-07); "continue until fully".** Foundation-first, reusing E2 (telemetry ingestion) + E4 (correlation) patterns; reduced-concept discipline held (no reified SecurityEvent — observations are append-only FACTS like telemetry samples; detections/correlation are derived).
- [x] E8.1a **Security observation ingestion foundation** — DONE (ADR-SOC-001): new append-only `security_observation` TimescaleDB hypertable + `POST/GET /v1/admin/security-observations` (PermAdmin), reusing telemetry patterns exactly — FORCE RLS `tenant_isolation` (fail-closed), in `TenantOwnedTables`, natural key carries tenant_id, tenant-scoped `appPool` ingestion with asset_id re-verification. Sibling table NOT telemetry_sample reuse (security facts are categorical/attributed, not numeric); a FACT not a reified event (no status/lifecycle/update/delete). Pluggable `SecurityObservationRepository`. Gate: two-tenant isolation + RLS/uniqueness/tenant-column arch guards all --- PASS (re-run independently, non-vacuous); hypertable confirmed; contract bijection PASS; make test (race) green; lint clean; migrate-hash/validate clean; PKG census updated with real numbers.
- [x] E8.1b-1 **`security_rule` detection config + CRUD** — DONE (ADR-SOC-002): tenant-owned `security_rule` config (threshold-over-window SIEM detection: count observations of `observation_type` ≥ `min_severity` on an asset in `window_seconds` ≥ `threshold_count` → raise `incident_severity` incident) mirroring `alert_rule` exactly — FORCE RLS, appPool CRUD, asset re-verify, PATCH row_version/DELETE-none (ADR-HARD-003), `last_state`/`current_incident_id` evaluator-owned (staged, unwritten). Two distinct severity vocabularies kept distinct (observation vs incident). Gate: two-tenant isolation + RLS/uniqueness guards --- PASS (re-run independently); contract PASS; make test (race) green; lint 0; migrate clean; PKG census updated.
- [x] E8.1b-2 **SIEM detector worker + correlation** — DONE (ADR-SOC-003): leader-gated `security.Detector` mirroring `alerting.Evaluator` (Config/Store/Run/RunOnce, transition-only) — counts observations via a SQL `CountForTenant` over the window, fires on threshold (writes `last_state`/`last_transition_at`/`current_incident_id`), and raises a SECURITY-sourced incident via a new `FindOrCreateOpenSecurityIncident` with its OWN partial unique index `ux_incident_open_security_per_asset` (no-duplicate under concurrency, proven; alert index untouched). Security & alert incidents coexist on the same asset. Recovery appends a note + clears the link, never auto-resolves (matches the evaluator). Split privileged store types to respect the privileged-read guard cap (**0 new exemptions**). Gate: security no-duplicate-concurrency + coexistence + tenant-isolation + **alert-path regression** all --- PASS (re-run independently, explicit non-vacuous); contract PASS (0 breaking); make test (race) green; lint 0; migrate clean; PKG census updated.
- **✅ E8.1 COMPLETE — SOC detection loop OPERATIONAL:** ingest observations (E8.1a) → threshold/window detection (E8.1b-1 config + E8.1b-2 detector) → security-sourced incident → the existing NOC loop (group/page/escalate). Reduced-concept held throughout (no reified SecurityEvent/Alert/Detection). Follow-ups: E9.1 incident-trends has no dedicated `security` source bucket yet (small NOC-dashboards fix); no SOC console UI yet (SOC incidents already appear on the incident board since they ARE incidents).
- [~] E8.2 Threat detection (IOC matching; behavioral/anomaly DEFERRED to E13/AI — needs baselines/ML)
  - [x] E8.2a **IOC watchlist store + CRUD** — DONE (ADR-SOC-004): tenant-owned `ioc` reference data (`indicator_type` ip|domain|url|file_hash|email + normalized `indicator_value` + `severity` + enabled/description/source), `POST/GET/PATCH/DELETE /v1/admin/iocs` (PermAdmin), mirroring security_rule CRUD. Tenant-scoped `UNIQUE (tenant_id, indicator_type, indicator_value)` (two tenants can watch the same indicator; dup within a tenant → 409); FORCE RLS; type/value not patchable. Gate: two-tenant isolation + tenant-scoped-unique + RLS/uniqueness guards --- PASS (re-run independently); contract PASS; make test green; lint 0; migrate clean; PKG census updated.
  - [x] E8.2b **IOC-match detection → incident** — DONE (ADR-SOC-005): sibling leader-gated `IOCMatcher` worker (mirrors `Detector`), value-based normalized match (observation attribute value ∈ tenant's enabled-IOC set, normalized per type), trailing window==interval (each observation ~once, no note-spam), reuses `FindOrCreateOpenSecurityIncident` (one open security incident per asset, existing no-dup index) at the IOC's severity with an indicator-naming note. Bounded (only tenants-with-enabled-IOCs, keyset-paged/capped); **0 new privileged-read exemptions**. Honest bounds: value-match not type-aware-field-mapping; first-writer-wins severity. Gate: match/non-match/disabled/normalization/no-dup + **two-tenant isolation** (shared indicator + shared asset_id) + privileged-read guard all --- PASS (re-run independently); make test (race) green; lint 0; build/vet clean.
- **✅ E8.2 COMPLETE — threat detection (IOC) OPERATIONAL:** curated IOC watchlist (E8.2a) + value-based IOC matching → security incidents (E8.2b), reusing the SOC correlation. Behavioral/anomaly detection remains DEFERRED to E13/AI (baselines/ML).
- [~] E8.3 Vulnerability management
  - [x] E8.3a **Vuln-finding entity + scan ingestion + lifecycle** — DONE (ADR-SOC-006): tenant-owned stateful `vuln_finding` (dedup `UNIQUE(tenant,asset,vuln_id)`; severity none|low|medium|high|critical CVSS scale; lifecycle open↔{remediated,accepted_risk,false_positive}). `POST/GET/PATCH /v1/admin/vuln-findings` (PermAdmin) — batch scan UPSERT (idempotent; refresh+reopen remediated/accepted on reappearance; **false_positive protected ATOMICALLY**, never overturned by a scan, only manual PATCH), row_version-guarded transitions, asset re-verify, FORCE RLS. Gate: isolation + false-positive-protection + idempotency + reappearance + RLS/uniqueness guards --- PASS (re-run independently); contract PASS; make test green; lint 0; migrate clean; PKG census updated.
  - [x] E8.3b **Prioritization + remediation-to-incident** — DONE (ADR-SOC-007): `GET /v1/admin/vuln-findings/prioritized` — computed projection ranking open findings by `severityRank × criticalityRank` (both 1-based so `unknown`<`low` and a high-sev finding on an untiered asset isn't zeroed), tie-break last_seen/finding_id, RLS-appPool, bounded, NO stored priority. `POST /v1/admin/vuln-findings/{id}/remediate` — opens/returns one `IncidentSourceVuln` remediation incident per finding (link column `remediation_incident_id`), idempotent + concurrency-safe via `SELECT FOR UPDATE` on the finding (10-goroutine test → exactly one incident); reuses `recordEvent` (arch audit-append guard enforced). Gate: concurrency-idempotency + ranking + isolation + audit-append guard --- PASS (re-run independently); contract PASS; make test green; lint 0; migrate clean; PKG census updated.
- **✅ E8.3 COMPLETE — vulnerability management OPERATIONAL:** stateful findings + idempotent scan UPSERT (false-positive-protected) → severity×CI-criticality prioritization → one-click remediation incident. Auto-close-on-absence still deferred.
- [~] E8.4 Compliance + Risk (continuous-audit automation DEFERRED — speculative; standing customer-validation caveat)
  - [x] E8.4a **Risk register + scoring** — DONE (ADR-SOC-008): tenant-owned stateful `risk` (likelihood rare..almost_certain, impact negligible..severe, treatment mitigate|accept|transfer|avoid, nullable asset_id; lifecycle open↔{mitigating,accepted,closed}, row_version-guarded, no Delete). `POST/GET/PATCH /v1/admin/risks` + `GET /v1/admin/risks/register` — computed likelihood×impact score (1-based, never stored), RLS-appPool, bounded. Ownership + source_finding_id deferred (justified). Gate: isolation + register + transitions + RLS/uniqueness guards --- PASS (re-run independently); contract PASS; make test green; lint 0; migrate clean; PKG census updated.
  - [x] E8.4b **Compliance controls + evidence** — DONE (ADR-SOC-009): tenant-owned stateful `compliance_control` (framework + control_ref [natural key, immutable] + title + status lifecycle not_implemented↔in_progress↔implemented / →not_applicable, row_version-guarded, no Delete) + `control_evidence` — an IMMUTABLE append-only child (kind url|note|attestation) hardened exactly like incident_event (ENABLE ALWAYS triggers + REVOKE writes + immutableAuditTables, swept by the audit-append guard). `POST/GET/PATCH /v1/admin/compliance-controls` + `POST .../{id}/evidence`; evidence embedded in the control GET (bounded). Cross-tenant append impossible (parent FOR UPDATE = RLS re-verify). Gate: isolation + audit-append + audit-immutability + RLS/uniqueness guards --- PASS (re-run independently); contract PASS; make test green; lint 0; migrate clean; PKG census updated.
- **✅ E8.4 COMPLETE — compliance + risk:** risk register + likelihood×impact scoring (E8.4a) + compliance controls with an immutable evidence trail (E8.4b). Continuous-audit automation DEFERRED (speculative). **Standing caveat (ADR-SOC-008/009): this is the least customer-validated SOC surface — built per directive, value unproven until a customer with a real compliance obligation exercises it.**
- [x] E8.5 Security automation — SAFE-slice SOAR — DONE (ADR-SOC-010): `security_response_rule` config (condition min_severity + optional asset → ONE SAFE ActionSpec) with `action_type` allowlisted to `http|notification` at BOTH domain validation AND a DB CHECK (command/destructive refused, adversarially tested). Leader-gated `security.Responder` scans new security incidents per tenant and runs the safe action via the EXISTING `policy` action Registry — exactly-once via an immutable `security_response_dispatch` ledger (record-first/at-most-once; claimed under the incident's FOR UPDATE). **Constitutional audit chain + `ConfigurationOperation` UNTOUCHED** (verified empty diff — no operational-event pollution of the governance ledger). NOT a reified Workflow/Playbook. **Destructive/autonomous response DEFERRED behind the machine-action attestation model** (the corpus's #2 finding / "price the autonomy" — a strategy-level decision, not a story). Gate: allowlist-rejection (domain+DB) + exactly-once + two-tenant isolation + privileged-read guard (0 new exemptions) + audit-append guard + audit-chain-untouched all --- PASS (re-run independently); contract PASS; make test green; lint 0; migrate clean; PKG census updated.
- **✅ E8 SOC EPIC COMPLETE (2026-08-07):** E8.1 ingestion→detection→incident · E8.2 IOC threat detection · E8.3 vuln management (findings/scan-upsert/prioritization/remediation) · E8.4 compliance controls + risk register · E8.5 SAFE-slice SOAR. ADRs SOC-001..010. Reduced-concept held throughout (no reified SecurityEvent/Alert/Workflow/Playbook); alert path + audit chain untouched; every story two-tenant-isolation-proven + gated. **DEFERRED (customer/strategy-gated): behavioral/anomaly detection (E13/AI), continuous-audit automation, destructive/autonomous SOAR response (attestation model). Standing caveat: compliance/risk/SOAR are the least customer-validated surfaces — built per directive, value unproven until a customer exercises them.**
- **Edge cases:** security-data isolation & retention/legal hold, chain of custody, high-severity fast-path.

### E-SEC-UI — Security console (make the SOC backend demonstrable)  `[~]` — **CTO decision under full control (2026-08-07):** the E8 SOC backend is complete but invisible; a demo is the bridge to customer evidence (standing charter). Surface it in the Cloudscape console over the EXISTING E8 endpoints (frontend-only), gated + live-visual-verified, mirroring the E-ACT/E7 patterns. New "Security" nav section.
- [x] E-SEC-UI.1 **Vulnerabilities screen** — DONE: Security nav section + `/security/vulnerabilities` — findings table (filter status/severity, severity/status StatusIndicators, scanner, remediation-incident link) + a **Prioritized** segmented view (risk-ranked by severity×CI-criticality, showing priority band + asset criticality) + legal-only status transitions + one-click **Remediate** (idempotent, disabled when linked) + SplitPanel detail + 403 PermissionNeeded. Over existing `/v1/admin/vuln-findings{,/prioritized,/{id},/{id}/remediate}` (no Go). Gate: web-test 207/207; TS shapes mirror handlers/openapi; self-contained clean; **CTO visual gate PASS — live, seeded data: All-findings + Prioritized views both render; the risk blend is visibly correct (a High-sev vuln on a low-criticality asset ranks below Medium items).** ▶ CURRENT
- [x] E-SEC-UI.2 **Detection** — DONE: two screens under Security — **Detection rules** (`/security/detection-rules`, security_rule CRUD: asset·observation_type/min_severity/"N in Ws" threshold/incident_severity/state/enabled/linked-incident, asset+type fixed at create) + **Indicators** (`/security/indicators`, ioc CRUD: type/value/severity/enabled/source/description, type+value fixed at create, dup→409 inline). Both write-action pattern (row_version, confirm-delete, refetch, 409, RFC 7807), enum Selects constrained to real value sets, 403→PermissionNeeded. Over existing `/v1/admin/security-rules` + `/iocs` (no Go). Gate: web-test 243/243 (36 new); shapes mirror handlers/openapi; self-contained clean; **CTO visual gate PASS — live (seeded rule + 2 IOCs): both boards render correctly.**
- [x] E-SEC-UI.3 **Risk & Compliance** — DONE: **Risk register** (`/security/risks` — risk CRUD + lifecycle, All-risks board + a **Register** segmented view ranked by computed likelihood×impact score with severity band) + **Compliance** (`/security/compliance` — control CRUD + legal-only status lifecycle + an **append-only evidence trail** [kind/value/recorded_by/time, newest-first] with an Add-evidence form; framework/control_ref immutable, no evidence edit/delete). Both write-action pattern, enum Selects constrained, 403→PermissionNeeded. Over existing `/v1/admin/risks{,/register}` + `/compliance-controls{,/{id}/evidence}` (no Go). Gate: web-test 267/267 (24 new); shapes+lifecycle-maps mirror handlers/openapi; self-contained clean; **CTO visual gate PASS — live (seeded 2 risks + a control w/ 2 evidence): Register view scores correctly (Likely×Severe=20 Critical), evidence trail + append render, legal-transition gating correct.**
- [ ] E-SEC-UI.4 **Response rules + Observations** — SOAR response-rule management over `/v1/admin/security-response-rules` + a read view of `/v1/admin/security-observations`. ▶ CURRENT

### E9 — Reports & Dashboards (projections)  `[ ]`
- [ ] E9.1 Query/aggregation engine over the domain + telemetry (bounded, cached)
- [ ] E9.2 Dashboards (composable projections), scheduled reports, exports
- [ ] E9.3 SLO/KPI reporting
- **Edge cases:** large aggregations, per-tenant data, caching/staleness, export volume.

### E10 — Knowledge Management  `[ ]` (extends PKG)
- [ ] E10.1 Knowledge articles / runbooks / known-errors, linked to CIs/incidents/problems
- [ ] E10.2 Versioning, review workflow (reuse governance), lifecycle
- (Semantic search arrives in E13.)

### E11 — Real-time & Integration platform (cross-cutting enablers)  `[ ]`
Pull forward as soon as E7/dashboards need it.
- [ ] E11.1 Streaming/real-time layer (WebSocket/SSE) with tenant-scoped subscriptions
- [ ] E12.x Connector framework: inbound (monitoring tools, cloud providers, SNMP, syslog) + outbound (ITSM/chat/paging) — versioned, sandboxed, SSRF-guarded
- [ ] Search: cross-entity search service (keyword first; semantic in E13)
- **Edge cases:** subscription fan-out at scale, connector failure isolation, backfill.

### E12 — Project & Portfolio Management  `[ ]`
- [ ] E12.1 Projects, tasks, dependencies, timelines, resourcing
- [ ] E12.2 Portfolio rollups (feeds E14 executive)
- **Edge cases:** cross-project dependencies, capacity conflicts.

### E13 — AI & Automation (AIOps / SecOps / Copilot)  `[ ]` (needs an AI-architecture ADR first)
- [ ] E13.0 **AI architecture decision (ADR):** model strategy, RAG over platform data, guardrails, human-in-the-loop, cost/latency, data-privacy/tenant-isolation of prompts. **Blocks all of E13.**
- [ ] E13.1 Semantic search over knowledge + CMDB + incidents
- [ ] E13.2 Copilot (operator assist: summarize incident, suggest next action, draft RCA) — human-in-the-loop
- [ ] E13.3 Predictive analytics (capacity forecasting, anomaly/failure prediction)
- [ ] E13.4 Auto-remediation / SOAR (AI-proposed, policy/approval-gated actions via workflow)
- **Edge cases:** hallucination guards, never auto-act without a gate, prompt/tenant data isolation, cost controls, audit of AI actions.

### E14 — Executive Command Center  `[ ]` (projections over everything)
- [ ] E14.1 Executive dashboards, KPI monitoring, portfolio view
- [ ] E14.2 Cross-business insights, enterprise analytics, decision intelligence
- **Edge cases:** cross-tenant aggregation only where authorized; data freshness; drill-down authorization.

### Capacity Management (spans E2/E9/E13)
- [ ] Capacity/utilization tracking, forecasting, right-sizing recommendations. Sequenced with E9/E13.

---

## 6. Global decisions still OPEN (resolve via ADR before the dependent epic)
- ~~**D1 — Time-series storage** (E2): Postgres partitioning vs dedicated TSDB.~~ **RESOLVED** — ADR-TELEMETRY-001: Postgres + TimescaleDB hypertables behind a pluggable interface (RLS preserved, one datastore/DR, swap-later hedge).
- **D2 — Real-time transport** (E7/E11): WebSocket vs SSE; subscription model.
- **D3 — AI architecture** (E13): model, RAG, guardrails, isolation, cost.
- **D4 — Connector isolation/runtime** (E11/E12): in-process vs sandboxed/out-of-process.
- (Record each as an ADR when reached; link here.)

## 7. Working agreement (the anti-lost-track rules)
- One `▶ CURRENT` per active chain; update it and the checkboxes when work lands.
- Every increment: ADR if it's a new decision; two-tenant isolation test; contract
  bijection; load/latency note; runbook if alertable; PKG census updated.
- Branch naming: `feat/<epic>-<slug>` (e.g. `feat/asset-cmdb`). Stack within an
  epic; consolidate to `master` per epic once reviewed.
- This file is updated in the SAME change that lands the work. No exceptions.

## 8. Standing note (CTO → founder)
The platform capability is real and compounding. The remaining constraint is not
engineering but a paying design partner — the highest-leverage move, and the one
outside the team's lane. Everything above is buildable; sequence and depth are
tuned so early customer feedback can reprioritize without rework (foundation is
customer-agnostic; intelligence/executive layers are where opinions will differ).
